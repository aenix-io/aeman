/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/board": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The board: title, the team roster, the Project board's structure, the people and the visitor's domains.
         * @description The board is the one the server was started with — there is no addressing parameter, and "project" is aeman's own planning entity. Ask it with the same day selector as the listing beside it: a past day's SPRINT POINTERS come with that day's cards, and today's pointers over that day's cards drop nearly all of them.
         */
        get: operations["getBoard"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/views": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The boards a caller may open, each with the gestures it draws. */
        get: operations["listViews"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/views/{view}/cards": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the cards that board draws, in board order.
         * @description A listing is the board-ROW shape: no `spec.description` — `status.links` carries the refs extracted from it — and the body itself is one GET /cards/{uid} away. `fields=full` opts a genuine bulk reader into complete cards. Every listing is the visitor's projection: cards in domains they cannot read are not there.
         */
        get: operations["listCards"];
        put?: never;
        /**
         * Create the card that board makes.
         * @description A create means what the board means by it: typed into `me` the card is yours, today, in the sprint and in the unplanned band; into `team` it lands in the Unassigned column unless somebody is named; into `triage` it is scheduled for a week; into `backlog` it is parked on the team's shelf; into `project` it is a slot under a column. Each board refuses the fields it does not own — a parked card typed into a day, a column named on the Me board — with a 422 that says which field it was. A title that is nothing but a GitHub issue/PR URL becomes that item's real title, with the link moved into the description.
         */
        post: operations["createCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/views/{view}/watch": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Watch that board over a WebSocket.
         * @description An upgrade, not a response: each text frame that follows is one `#/components/schemas/WatchFrame`. With the narrowing parameters the subscription is SCOPED — a card entering the selection arrives as ADDED and one leaving it as DELETED — while `view=all` with no selectors is the raw board stream. `resources` picks the kinds and `client` keys echo suppression: a change made with the same `X-Aeman-Client` is not streamed back on that connection.
         */
        get: operations["watchView"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/views/{view}/cards/{uid}/actions/remove": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * The board's × — hand the card back to a home it still has, or delete it by that board's rules.
         * @description `intent` says which of the ×'s two meanings is wanted: `unassign` empties the working area and leaves the card in its week or its column, `off-board` takes it away with the subtasks that were pieces of it. Omitted, the gesture decides for itself. `off-board` on a Project-board slot or a process turn is 422 — those belong to another board's plan; `unassign` on a card with no week and no column is 422 too, since it would leave the card nowhere at all.
         *
         *     From the Me board the × is the narrow one: a person takes off their own board what they PUT there and what the plan has not taken up. Work another person scheduled for them, or work they scheduled on the Team or Triage board, is somebody's plan and is refused there (403) — the wide × lives on the board where the planning was done, and the answer to "I am not doing this" is `spec.stage: refuse`. A card with no recorded author is not exempt, and a subtask is outside the rule.
         *
         *     The × empties the WORKING AREA: assignee, sprint and dates cleared (a slot keeps its dates — they are its row). The card then lands wherever it still belongs — the week it is scheduled for, or its Project-board column. With nowhere else to be the working area was its only home, and the × deletes it. It takes the card's subtasks with it; a subtask standing in a column of its own is freed into that column instead, and one whose column cannot come along (a column of the parent's repository) is answered like any other columnless card, which is deletion. A deleted card is not lost to the board's history: the day it stood on gives it back. DELETE /cards/{uid} is the deliberate delete, and it FREES a card's subtasks rather than taking them along.
         *
         *     A second, wider rule sits in front of both doors, this one and DELETE /cards/{uid}: a card the caller is CARRYING but did not create is not theirs to take off the board, on ANY board (403). Their answer to work somebody else planned for them is `spec.stage: refuse`, which leaves the card standing for the person who planned it to decide. Unlike the Me-board rule above, a card with no recorded author is outside this one — there is nobody else it could belong to — and so are a subtask and a review card, which are pieces of other work rather than work assigned to anyone.
         *
         *     A client should ask before pressing it when there is progress or a linked review card to lose: the server does not ask, and what the card carries does not change what the gesture does.
         */
        post: operations["removeCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/views/{view}/cards/{uid}/actions/place": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * The Triage board's drop — put the card in a week, which is what triaging it means.
         * @description `week` is a Monday; any other day is refused (422), since a week that is not a Monday matches no column the board draws. A week AHEAD of this one also empties the working area — the card is on no day board until that Monday — while the CURRENT week joins the team's sprint instead. A Project-board slot is not refused: its week IS its start date's week, so its DATES are what move — the whole span shifts by the distance to the week asked for, and the row follows. It is not the same as PATCH {"week": …}, which writes the week and nothing else and refuses a slot outright (422 `weekDerived`).
         */
        post: operations["placeCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/views/{view}/cards/{uid}/actions/untriage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * The Triage board's opposite — take the card's week away, back to the strip.
         * @description A Project-board slot is refused (422 `weekDerived`): its week is its row, derived from its dates, so there is no week to take away — taking it off the column is the gesture that means this. A card that is in no week already is left as it is.
         */
        post: operations["untriageCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/views/{view}/cards/{uid}/actions/finished-earlier": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * A day board's answer for work done in the sprint before this one and marked done now.
         * @description It sends finished work back to the sprint it was done in, its dates and the day it counts as done along with it. A MOVE rather than a removal, which is why it has a door of its own.
         */
        post: operations["finishedEarlier"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** One card in full — the body lives here, not in listings. */
        get: operations["getCard"];
        put?: never;
        post?: never;
        /**
         * Hard delete, cascading to the linked review card.
         * @description The deliberate delete — "this card is a mistake" — as against the board's ×, which is "this work is off the board". It FREES the card's subtasks into standalone cards rather than taking them along. A card the caller is CARRYING but did not create is refused here as it is at the × (403): `spec.stage: refuse` is the answer to work somebody else planned. A subtask, a review card and a card with no recorded author are outside that rule.
         */
        delete: operations["deleteCard"];
        options?: never;
        head?: never;
        /**
         * Edit spec fields; only present fields apply, and an empty value clears.
         * @description The server applies the rules as the UI would: the clamps, the review linkage, the calendar semantics of the dates. Fields are applied in the order the handler reads them, and the assignees last — ungrouping hands an ownerless child its parent's person, and a patch that also names assignees means the caller decided who owns it.
         */
        patch: operations["patchCard"];
        trace?: never;
    };
    "/cards/{uid}/actions/move": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reorder the card after (or before) another one.
         * @description `before` wins when both are sent; an empty `after` puts the card at the top.
         */
        post: operations["moveCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/defer": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Push the scheduled day N days ahead of today.
         * @description Presses stack, and a card created today relocates fully. `days` must be positive — putting work OFF is the whole gesture — and a zero or negative one is 422.
         */
        post: operations["deferCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/in-progress": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Move the card to the implicit In Progress status. */
        post: operations["setInProgress"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/reopen": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Undo a done mark, restoring the progress the card had when done was set.
         * @description The stage clears and the progress returns to what the card carried when it was marked done, the jump recorded in its log; with no history to read it falls back to In Progress.
         */
        post: operations["reopenCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/send-to-review": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send the card to a reviewer, or reassign the review that exists.
         * @description The answer is the REVIEW card, whether it was created or reassigned. A reviewer who cannot read the card's repository cannot see the review either — it lives with the card it reviews — which is what `domains[].members` is for: a client narrows its picker by it. Without a `zone` the review lands in the unplanned one: for the reviewer it is work that turned up during their day, and the original's band says where the WORK stood, not where the asking belongs.
         */
        post: operations["sendToReview"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/mirror": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Show the card in a second Project-board column.
         * @description One card, one file and one log, standing in both plans. The card must already be in a column; the target must exist, in the card's own repository, and must not be the card's own column; a subtask cannot be mirrored (all 422). Mirroring where it already stands is a no-op. The EPIC half is required — a column is named by its epic — while the project half may be empty, since the no-project bucket is a mirror home like any other.
         */
        post: operations["mirrorCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/unmirror": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Take one mirror column away; the home and everything else stay. */
        post: operations["unmirrorCard"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/remove-from-project": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * The Project board's × — take the card out of one column.
         * @description A mirror goes; the home with mirrors left hands its role to the first mirror; the last column drops the WEEK that was the slot's row, keeps a worked card (assignee and progress) as an orphan of the working area, and deletes the rest, cascading the linked review card. A SUBTASK is never deleted here — its other home is its parent, so the × only takes the column away. It answers 204 rather than the card, which may no longer exist.
         */
        post: operations["removeFromProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/actions/remove-reviewer": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Delete the linked review card. */
        post: operations["removeReviewer"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/links": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The URLs found in the card's description, GitHub refs resolved and listed first.
         * @description Only github.com references are recognised; GitLab issue and merge-request links are plain links for now.
         */
        get: operations["listLinks"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/log": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The card's activity feed — recorded events and work notes, one chronological list.
         * @description Read from the repository's history: every commit that touched the card is one or more events, with the commit's author and time, merged with its notes. A change made by a direct git write shows up like any other. This is one card's whole history; for a day's feed across many cards ask GET /logs.
         */
        get: operations["getCardLog"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/logs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One day's feed for many cards at once — what a day board shows.
         * @description The history walk stops at the day's first moment, so this costs a fraction of a log per card: asking a whole log per card is what made a board open fire dozens of slow requests. A card that was quiet that day is present with an empty list; a card the visitor cannot read is absent.
         */
        get: operations["getDayLogs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/notes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The card's work notes. */
        get: operations["listNotes"];
        put?: never;
        /**
         * Append a work note.
         * @description Every note mutation answers the card's whole thread, so clients converge on the server's view of it rather than on their own idea of the order.
         */
        post: operations["addNote"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/cards/{uid}/notes/{noteId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete a work note. */
        delete: operations["deleteNote"];
        options?: never;
        head?: never;
        /** Edit a work note. */
        patch: operations["editNote"];
        trace?: never;
    };
    "/sprints": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-team sprint pointers.
         * @description Ask it with the same day selector as the listing beside it: a past day's pointers come with that day's cards, and the client's own view rules compare a card's sprint against them.
         */
        get: operations["listSprints"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Set a team's sprint pointer directly.
         * @description A team is declared by its first sprint write, in the `domain` the caller picks; an existing team's pointer stays where the team is.
         */
        patch: operations["patchSprint"];
        trace?: never;
    };
    "/people/{login}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Set the points a week a PERSON gets through.
         * @description The number a day board measures their load against. 0 takes a set number back and nothing derives one; outside 0..999, or a login that could not be one, is 422. It answers the whole Board resource, whose members carry `load` and `capacity` — that is what the client redraws.
         */
        patch: operations["patchPerson"];
        trace?: never;
    };
    "/sprints/actions/carry-over": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Advance a team's sprint to today and carry its unfinished cards.
         * @description Finished recurrent cards reseed as fresh copies. With `dryRun` the counts are reported and nothing changes, which is what backs a confirm dialog.
         */
        post: operations["carryOver"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/sprints/actions/reorder-teams": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Apply a shared team order.
         * @description The hidden sprint-state cards are moved into the given sequence, so every client reads the same order back from the board metadata. Teams missing from the list keep their positions after the reordered ones.
         */
        post: operations["reorderTeams"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/sprints/actions/delete-team": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Delete a team's sprint pointer; refused while cards still use the team. */
        post: operations["deleteTeam"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/epics": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Declare an epic column.
         * @description The project is OPTIONAL, and the schema beside this says so too: named, it must already exist — a project the board does not have is a typo, and is refused (422 `projectNotFound`) — and the column lives with it; left empty, the column goes in the no-project bucket, which is a column home like any other. The name has to be free inside whichever of the two it lands in (422 `epicExists`).
         */
        post: operations["addEpic"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/epics/actions/delete-epic": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Delete an EMPTY epic column; refused while cards sit under it. */
        post: operations["deleteEpic"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/epics/actions/reorder-epics": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Apply one project's column order. */
        post: operations["reorderEpics"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/epics/actions/set-project": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Move a column from one project to another; an empty target detaches it.
         * @description Its cards are rewritten with it. The column cannot change REPOSITORY — its stub is written back to the repository that holds it — so a target project of another one is refused (422), and the name must be free in the destination, the no-project bucket included.
         */
        post: operations["setEpicProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/epics/actions/rename": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rename a column in place, cards and all.
         * @description A name already used inside the SAME project is refused (422); the same name in another project is fine.
         */
        post: operations["renameEpic"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The Process tab — every process with its tasks and each task's history. */
        get: operations["listProcesses"];
        put?: never;
        /**
         * Declare a process — recurring work inside a project.
         * @description A process with a project lives with the project; `domain` picks the repository for a project-less one.
         */
        post: operations["addProcess"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/actions/delete-process": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Delete an EMPTY process; refused while it has tasks. */
        post: operations["deleteProcess"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/actions/rename": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Rename a process; its tasks follow. */
        post: operations["renameProcess"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/actions/set-project": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Move a process to another project; an empty project is the no-project bucket. */
        post: operations["setProcessProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/actions/set-paused": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Pause a process, or resume it.
         * @description A paused process files no turns and projects none; resuming files what the current week is owed.
         */
        post: operations["setProcessPaused"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/actions/reorder": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Apply a shared process order. */
        post: operations["reorderProcesses"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/tasks/actions/reorder": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Apply one process's task order.
         * @description A uid from another process is adopted into this one — which is how a cross-process drop lands.
         */
        post: operations["reorderProcessTasks"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/tasks": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Add what a process iterates on.
         * @description A task says what every turn is called and says, its cycle (counted on the calendar from its start date, not from when the last turn closed), the team the turns land in and the standing owner. The server files what each week is owed by itself, as the week arrives.
         */
        post: operations["addTask"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/processes/tasks/{uid}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete a task; its past turns stay as the record. */
        delete: operations["deleteTask"];
        options?: never;
        head?: never;
        /** Change what the NEXT turns will be; the running one is untouched. */
        patch: operations["patchTask"];
        trace?: never;
    };
    "/deadlines": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mark a week with a project's deadline line.
         * @description A project holds at most one per week; any day resolves to its Monday, and asking twice changes nothing. Two projects can each have something due the same week, and those are two lines.
         */
        post: operations["addDeadline"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/deadlines/actions/delete": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Clear a project's deadline on a week. */
        post: operations["deleteDeadline"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/deadlines/actions/move": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Drag a project's deadline to another week.
         * @description Landing where it already has one leaves a single line; another project's line on that week is untouched.
         */
        post: operations["moveDeadline"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/projects": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Declare a project — the Project board's top grouping, which owns epic columns.
         * @description It may be created empty; `domain` picks the repository it is declared in.
         */
        post: operations["addProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/projects/actions/delete-project": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Delete an EMPTY project; refused while it still owns columns. */
        post: operations["deleteProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/projects/actions/reorder-projects": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Apply the shared project order. */
        post: operations["reorderProjects"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/projects/actions/rename": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Rename a project in place; its columns and their cards follow. */
        post: operations["renameProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/teams/actions/rename": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rename a team in place, its cards and process tasks along with it.
         * @description Its declaration keeps its sprint pointer. A name another team has is refused (422), and the no-team group cannot be renamed.
         */
        post: operations["renameTeam"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/teams/actions/capacity": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Set the points a week a team gets through.
         * @description The number a week of the team's plan is weighed against, stored in the team's own file. 0 takes it back and the board derives none; outside 0..999 is 422, and so is a team the board does not declare.
         */
        post: operations["setTeamCapacity"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/presence": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Share the caller's live card selection; an empty card clears it.
         * @description Ephemeral shared-cursor state, broadcast over the watch and never persisted. The login is the caller's authenticated identity, not a client-supplied value, and the client id (`X-Aeman-Client`) keys it, so a closed tab clears its own mark.
         */
        post: operations["setPresence"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/openapi.json": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** This document, as JSON. */
        get: operations["getOpenAPI"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description An RFC 9457 problem detail, with two extension members. */
        Problem: {
            /** @description Always "about:blank": there is no page per problem, and `code` carries the identity. */
            type: string;
            /** @description The status code's own text. */
            title: string;
            /** @description The HTTP status, repeated in the body. */
            status: number;
            /** @description The sentence to show a person. */
            detail: string;
            /**
             * @description The rule that refused, for a program to branch on. For a rule of the board it is that rule's own name.
             * @enum {string}
             */
            code: "accessUndecided" | "authorizationExpired" | "backwardsDefer" | "badCapacity" | "badDay" | "bodyTooLarge" | "capacityRequired" | "cardNotFound" | "cardNotOnBoard" | "crossDomain" | "crossSiteBlocked" | "dayIsARecord" | "descriptionTooLong" | "domainConflict" | "emptyNote" | "emptyTitle" | "endBeforeStart" | "epicExists" | "epicInUse" | "epicNotFound" | "epicRequired" | "forbidden" | "gestureNotOffered" | "historyTruncated" | "invalidAsOf" | "invalidBody" | "invalidDay" | "invalidSelector" | "invalidStage" | "nameTaken" | "noColumn" | "noEarlierSprint" | "noHistory" | "noPlaceOfItsOwn" | "noReviewer" | "noSuchView" | "notAMonday" | "notAuthenticated" | "notFinished" | "notInProject" | "notOnThisBoard" | "notRecurrent" | "notYoursToDestroy" | "notYoursToPark" | "notYoursToRefuse" | "notYoursToRemove" | "noteNotFound" | "noteTooLong" | "nowhereToLeaveIt" | "openSubtasks" | "outsideCycle" | "ownColumn" | "parentNotFound" | "pointsRequired" | "processExists" | "processInUse" | "processNotFound" | "projectExists" | "projectInUse" | "projectNotFound" | "setupRequired" | "subtaskDepth" | "subtaskMirror" | "subtaskTie" | "subtaskWeek" | "taskNotFound" | "teamExists" | "teamInUse" | "teamNotFound" | "titleTooLong" | "tooManyCards" | "turnProcess" | "unknownDomain" | "unknownIntent" | "unknownRoute" | "unknownSize" | "unknownStage" | "unknownZone" | "upstreamFailed" | "viewNeedsField" | "weekDerived";
            /** @description Where the person can go to fix the refusal — installing the board's app — for a client to offer as a button. */
            actionUrl?: string;
        };
        Card: {
            /** @enum {string} */
            kind: "Card";
            metadata: components["schemas"]["CardMetadata"];
            spec: components["schemas"]["CardSpec"];
            status: components["schemas"]["CardStatus"];
        };
        /** @description The card's identity and immutable facts. */
        CardMetadata: {
            /** @description A ULID, assigned at creation and never changed by a rename, a move or a re-filing. A card migrated from a GitHub Projects v2 board also answers to its old id for one major version. */
            uid: string;
            author?: string;
            createdAt?: string;
        };
        /** @description The user's intent — everything an edit can change. */
        CardSpec: {
            title: string;
            /** @description The card's body. ABSENT in a listing — that is the "not loaded" marker — and always present, possibly empty, on a full resource, so an empty body never reads as a missing one. */
            description?: string;
            team?: string;
            /**
             * @description The band the card stands in, by meaning rather than by colour. A card in the WORKING AREA — one carrying a sprint or dates of its own — always answers with a band: nobody having said means "planned", which is where every board draws it anyway. Absent is reserved for cards that are on no day (the strip, a shelf, a week to come), where "no band yet" is a state a client draws as one.
             * @enum {string}
             */
            zone?: "urgent" | "unplanned" | "planned" | "niceToHave";
            /**
             * @description What somebody said the card weighs. The points a board sums are derived from it — S 1, M 2, L 4, XL 8 — never stored, and a card with SUBTASKS weighs their sum instead of its own size (the umbrella rule: a parent's estimate stands until its children exist, then they replace it, and nothing counts twice). A card nobody has sized weighs M — except a review card, which weighs S — and that default is what the SUMS use, never a value written to the card, so the field stays absent on it. A size the board does not know is answered as absent rather than echoed back.
             * @enum {string}
             */
            size?: "S" | "M" | "L" | "XL";
            assignees: string[];
            /** @description 0..100. The service clamps rather than refuses: review and locked hold it inside [10, 90], and done fills it. */
            progress: number;
            /** @description Open on the way out, unlike the patch's closed `stage`: a card's stage is read verbatim from its file in the repository (pkg/gitstore/layout.go), which any tool may commit to, so this reports what the board says rather than what a write would have been allowed to set. An explicit status that recolours the bar: locked, review, recurrent, refuse — or absent, the implicit in-progress state. Done is DERIVED (`status.complete`): 100% with no stage. `refuse` is the answer of the person the card is on, so only they may set it (403 otherwise); it does not take the card off anyone's board, it leaves it standing for their lead to answer, and clearing it IS that answer, which anyone may do. A review card takes neither review nor recurrent (422). Taking a card off review cancels its unfinished linked review card, unless the reviewer has already worked on it. */
            stage?: string;
            /** @description A recurrent card's reseed cycle: absent is every sprint, "week" and "month" once the interval has elapsed. */
            recurrence?: string;
            dates: components["schemas"]["CardDates"];
            /** @description The week a card with no dates is scheduled for, a Monday: its column on the Triage board. Any other day is refused (422) — a column IS a Monday. A slot's row is the week of its `dates.start`, always derived, so setting a week on a card filed under a column is refused too, though re-asserting the week it already derives is a harmless no-op. A process TURN keeps to its own occurrence. */
            week?: string;
            /** @description The card is on its team's SHELF rather than in the plan — work somebody has read and put aside. Exclusive with `week`: setting one clears the other, at every door. Parking empties the working area, takes the card's subtasks off the board with it, withdraws any unfinished review of it, and puts it at the top of the shelf. Every team has a shelf and nothing declares it. A Project-board slot, a process turn, a review card and a subtask are refused (422): none of them has a place of its own to be parked out of. */
            parked?: boolean;
            /** @description The card's column, which is the (project, epic) PAIR — epic names repeat across projects, so anything acting on a column names both halves. */
            epic?: string;
            project?: string;
            /** @description The additional columns the same card stands in — one file, one log, one set of dates — always in the card's own repository. */
            mirrors?: components["schemas"]["Placement"][];
            /** @description The process this card is a turn of, or the one it was tied to directly. A card on a day board carries it so a person can see what the work belongs to. */
            process?: string;
            /** @description The task a process turn was copied from. */
            task?: string;
            reviewOf?: string;
            /** @description On a subtask, the uid of the card it is grouped under. */
            parent?: string;
        };
        /** @description The card's date model: the scheduled start, the end of its visible range, and the sprint it belongs to. PATCHing `start` runs the calendar rule — the sprint follows the sprint that was active on the start day — so patch only `end` or `sprint` for a granular change. */
        CardDates: {
            start?: string;
            end?: string;
            sprint?: string;
        };
        /** @description Derived by the server, never written by a client. */
        CardStatus: {
            complete: boolean;
            inProgress: boolean;
            /** @description A promise made on ANOTHER board and broken: a slot past its end date, a process turn past the week its process filed it into. A card the Triage board scheduled into a week is not marked late for still being open — that week is this board's own planning — though it is still a debt. */
            overdue?: boolean;
            /** @description The assignee of the unfinished linked review card. */
            reviewedBy?: string;
            reviewRound?: number;
            /** @description The moment this card is a RECORD of: its team's sprint has moved past the day asked for, so this is what the card was that evening. Absent on every live card, including in the same listing — whether a day is over is each team's own answer. */
            asOf?: string;
            /** @description The repository the card lives in — the store's decision by the inheritance rule, never a client's choice. Read it as a NAME to compare against `domains[0]`, never as "absent means primary". */
            domain?: string;
            /** @description The board day the card reached 100, cleared on reopen. */
            doneAt?: string;
            /** @description The card stands in the Triage board's strip: nobody has placed it in a week and it is not being worked. */
            triage?: boolean;
            /** @description The Monday of the column the card stands in on the Triage board: its week, or — for a review card, which has none of its own — the week its own dates fall in. */
            triageWeek?: string;
            /** @description A RECURRENT card's weeks to come: the ones it comes round in over the planning horizon and no copy of it stands in yet. */
            due?: string[];
            cycle?: components["schemas"]["CycleWindow"];
            /** @description The board day the older × demoted the card into the previous sprint, which a record of that day gives it back by. Nothing writes it any more. */
            leftAt?: string;
            /** @description The references extracted from the description, unresolved and capped — they ride the status so a row that omits the description still knows it has links to show. GET /cards/{uid}/links resolves them. */
            links?: components["schemas"]["CardLinkRef"][];
        };
        /** @description A process turn's own occurrence: the first and last week it may stand in, both Mondays and both inclusive. It is sent rather than derived because the calendar is the server's, and a second implementation of "when is this next due" is a second answer waiting to disagree. */
        CycleWindow: {
            from: string;
            to: string;
        };
        /** @description One reference extracted from a description, unresolved. */
        CardLinkRef: {
            /**
             * @description What the URL points at. A closed set: pkg/board/links.go recognises a github.com issue and pull-request URL (and the owner/repo#number shorthand, which the live resolver corrects to `pull` when the number turns out to be one) and calls everything else a plain link.
             * @enum {string}
             */
            kind: "issue" | "pull" | "link";
            url: string;
            owner?: string;
            repo?: string;
            number?: number;
        };
        /** @description A Project-board column — the (project, epic) pair. */
        Placement: {
            project: string;
            epic: string;
        };
        CardList: {
            /** @enum {string} */
            kind: "CardList";
            items: components["schemas"]["Card"][];
            /** @description The moment a snapshot listing reflects (RFC3339) — the end of the day it was asked for. Absent on a live listing. */
            asOf?: string;
        };
        /** @description A team's sprint pointer; the empty team is the no-team group. */
        Sprint: {
            /** @enum {string} */
            kind: "Sprint";
            metadata: components["schemas"]["SprintMetadata"];
            spec: components["schemas"]["SprintSpec"];
        };
        SprintMetadata: {
            team: string;
        };
        SprintSpec: {
            current?: string;
            previous?: string;
            capacity?: components["schemas"]["SprintCapacity"];
        };
        /** @description What a week of the team's plan is weighed against: the points a week it gets through, a number somebody SET and never derived — absent when nobody has said. */
        SprintCapacity: {
            points?: number;
        };
        Note: {
            /** @enum {string} */
            kind: "Note";
            metadata: components["schemas"]["NoteMetadata"];
            spec: components["schemas"]["NoteSpec"];
        };
        NoteMetadata: {
            id: string;
            cardUid: string;
            author?: string;
            createdAt?: string;
            source: string;
        };
        NoteSpec: {
            text: string;
        };
        /** @description The board-level manual order — the uid list clients sort by. */
        Ordering: {
            /** @enum {string} */
            kind: "Ordering";
            spec: components["schemas"]["OrderingSpec"];
        };
        OrderingSpec: {
            uids: string[];
        };
        Board: {
            /** @enum {string} */
            kind: "Board";
            metadata: components["schemas"]["BoardMetadata"];
        };
        BoardMetadata: {
            title?: string;
            url?: string;
            /** @description The teams that have sprint pointers, in board order. */
            teams: string[];
            projects?: string[];
            /** @description The roster of processes, so a client can tell when it changed; the Process tab reads the full structure from GET /processes. */
            processes?: components["schemas"]["ProcessRef"][];
            deadlines?: components["schemas"]["DeadlineRef"][];
            epics?: components["schemas"]["EpicRef"][];
            /** @description Every distinct assignee on the board, in login order, with the numbers beside each — the people roster the pickers offer. Who may READ a repository is the other list, `domains[].members`. */
            members: components["schemas"]["Member"][];
            /** @description The repositories the visitor can read, primary first. The board lists its repositories whatever it spans, so a client reads a SINGLE entry as "no boundaries" — by the COUNT, never by the list being absent — and compares the stamps cards and columns carry against these names. */
            domains?: components["schemas"]["DomainInfo"][];
            /** @description The repository each team was declared in. A card's team and its project must live in the same repository — the project decides where the card lives, so a team from another one would leave the card where its own people cannot read it — and a write that would pair them is refused (422), so a client narrows its own pickers with these maps rather than offering a choice the server will reject. An entry missing from a map means the primary. */
            teamDomains?: {
                [key: string]: string;
            };
            /** @description The repository each project was declared in. */
            projectDomains?: {
                [key: string]: string;
            };
            /** @description The repository each process was declared in: a card may only be tied to a process of its own repository. */
            processDomains?: {
                [key: string]: string;
            };
        };
        /** @description One person on the board. The login is the identity everywhere; the name is for eyes, and a forge that has none sends only the avatar. */
        Member: {
            login: string;
            name?: string;
            avatarUrl?: string;
            /** @description How much open work is this person's right now, counted across EVERY team: a board is read through a filter and a person is not. */
            carrying?: number;
            /** @description The same work weighed in points. */
            load?: number;
            /** @description The points a week the person gets through — the roster's number, and only that, absent when nobody has set one. A day board draws load/capacity beside the person and goes red past it. */
            capacity?: number;
        };
        /** @description One readable repository of the visitor's board: whether they may write it, and which of the board's members can read it — so a reviewer picker offers only people who will see the card. */
        DomainInfo: {
            name: string;
            writable: boolean;
            members: string[];
        };
        /** @description One Project-board column: its name and the project that owns it. A client cannot compute the column's repository from the project — the same project NAME may be declared in two repositories with its columns merged under one entry — and it is the COLUMN that decides whether a card may stand in it. */
        EpicRef: {
            name: string;
            project?: string;
            domain?: string;
        };
        ProcessRef: {
            name: string;
            project?: string;
        };
        DeadlineRef: {
            week: string;
            project?: string;
        };
        ProcessList: {
            /** @enum {string} */
            kind: "ProcessList";
            items: components["schemas"]["Process"][];
        };
        Process: {
            name: string;
            project?: string;
            paused?: boolean;
            tasks: components["schemas"]["Task"][];
        };
        /** @description What a turn is copied from, plus how the last few went. */
        Task: {
            uid: string;
            title: string;
            description?: string;
            recurrence: string;
            start?: string;
            team?: string;
            assignee?: string;
            /** @description An open turn holds the next one back — the stuck card IS the process — unless the task accumulates, for work where unpaid months must pile up as separate cards. */
            accumulate?: boolean;
            /** @description The task's recent turns, oldest first, and only the tail: a process that has run for a year has fifty, and this list rides in every board frame. */
            history: components["schemas"]["Iteration"][];
            /** @description Every turn, the tail that is not sent included: turns == the length of history means nothing was left out. */
            turns: number;
            done: number;
            late: number;
            /** @description The weeks this task comes due in over the planning horizon with no turn of its own yet — what the process is going to file. A paused process sends none. */
            due?: string[];
        };
        /** @description One spawned card and how it went: "done" when it closed, "open" while it still runs inside its cycle, "late" once the week it was owed in has passed with it still open. */
        Iteration: {
            uid: string;
            week: string;
            /** @enum {string} */
            state: "done" | "open" | "late";
        };
        /** @description One item of a card's unified activity feed. */
        LogEntry: components["schemas"]["LogEventEntry"] | components["schemas"]["LogNoteEntry"];
        /** @description A recorded change — stage, progress, review, plan. */
        LogEventEntry: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "event";
            id: string;
            at?: string;
            actor?: string;
            /** @description What changed. */
            kind?: string;
            from?: string;
            to?: string;
        };
        /** @description A work note, in the same timeline as the events. */
        LogNoteEntry: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "note";
            id: string;
            at?: string;
            actor?: string;
            text?: string;
        };
        LogList: {
            /** @enum {string} */
            kind: "LogList";
            items: components["schemas"]["LogEntry"][];
            /** @description Present only when the history is cut: the clone is shallow, and older entries exist on the remote without being here. A request for a card created before the horizon deepens the clone back to its creation, bounded by the server's history cap. */
            truncatedBefore?: string;
        };
        DayLogList: {
            /** @enum {string} */
            kind: "DayLogList";
            day: string;
            /** @description A card's uid to its entries on that day, oldest first. */
            cards: {
                [key: string]: components["schemas"]["LogEntry"][];
            };
        };
        /** @description One URL from a card's description. A GitHub issue or pull request is resolved to its live title and state where the server can read it. */
        Link: {
            url: string;
            /**
             * @description What the URL points at. A closed set: pkg/board/links.go recognises a github.com issue and pull-request URL (and the owner/repo#number shorthand, which the live resolver corrects to `pull` when the number turns out to be one) and calls everything else a plain link.
             * @enum {string}
             */
            kind: "issue" | "pull" | "link";
            owner?: string;
            repo?: string;
            number?: number;
            title?: string;
            state?: string;
        };
        LinkList: {
            /** @enum {string} */
            kind: "LinkList";
            items: components["schemas"]["Link"][];
        };
        SprintList: {
            /** @enum {string} */
            kind: "SprintList";
            items: components["schemas"]["Sprint"][];
        };
        NoteList: {
            /** @enum {string} */
            kind: "NoteList";
            items: components["schemas"]["Note"][];
        };
        ViewList: {
            /** @enum {string} */
            kind: "ViewList";
            items: components["schemas"]["View"][];
        };
        /** @description One board a caller may open and the gestures it draws. It exists so a client can READ the surface instead of being told it in a description, which is how the two drifted apart in the first place. */
        View: {
            name: string;
            gestures: string[];
        };
        CarryReport: {
            /** @description The open plan cards from earlier weeks — the debts now showing on the target week's panel. They are not moved. */
            carried: number;
            reseeded: number;
        };
        TaskRef: {
            uid: string;
        };
        /** @description The card a board makes. Every field is optional here because what a board REQUIRES and what it refuses are the board's own rules, answered as a 422 that names the field. */
        CreateCardRequest: {
            title?: string;
            team?: string;
            /** @enum {string} */
            zone?: "" | "urgent" | "unplanned" | "planned" | "niceToHave";
            /** @description S, M, L or XL in any case; empty leaves the card unsized. */
            size?: string;
            /** @description Only the first is read — a card is one person's. */
            assignees?: string[];
            dates?: components["schemas"]["CardDatesInput"];
            /** @description Schedules the card for a WEEK (its Monday) instead of a day: no dates are set and no sprint is joined. */
            week?: string;
            /** @description Puts the card straight on its team's shelf. Like `week`, no dates are set and no sprint is joined. */
            parked?: boolean;
            /** @description With `project`, files the card on the Project board under the column that pair identifies. Its row is the week of `dates.start` — `week` anchors it when there are no dates — and the dates may span weeks. */
            epic?: string;
            project?: string;
            reviewOf?: string;
            /** @description Groups the card as a subtask. A `parent` and a `week` together are refused (422): grouping hands the week to the parent, so the pair asks for two contradictory things. */
            parent?: string;
            startNewSprint?: boolean;
            /** @description Schedules the card for its day without joining any sprint — a "next sprint" create; the next carry-over to reach its day adopts it. */
            noSprint?: boolean;
        };
        CardDatesInput: {
            start?: string;
            end?: string;
            sprint?: string;
        };
        /** @description Only present fields are applied, so absent and empty are different things: empty clears. */
        CardPatch: {
            title?: string;
            description?: string;
            team?: string;
            /** @enum {string} */
            zone?: "" | "urgent" | "unplanned" | "planned" | "niceToHave";
            /** @description S, M, L or XL in any case; empty takes the size back. */
            size?: string;
            assignees?: string[];
            progress?: number;
            /**
             * @description A name the board does not have is refused rather than stored. "done" clears the stage and fills 100; review and locked clamp the progress into [10, 90].
             * @enum {string}
             */
            stage?: "" | "locked" | "review" | "recurrent" | "refuse" | "done";
            recurrence?: string;
            /** @description The two halves of the card's column; empty clears. Epic names repeat across projects, so filing into another project's column names both, while naming only the epic stays inside the card's project. */
            epic?: string;
            project?: string;
            dates?: components["schemas"]["CardDatesPatch"];
            /** @description The week the card is scheduled for; empty takes it off the weeks. Giving a STANDING subtask a week pulls it out of its group first — that is what the request means — while clearing one is free. */
            week?: string;
            parked?: boolean;
            reviewOf?: string;
            /** @description Groups the card under another one; empty ungroups. */
            parent?: string;
            /** @description Ties the card to an existing process — the recurring shelf's counterpart of a column; empty clears. The process must live in the card's own repository; only a RECURRENT card takes a tie, and a process TURN is refused outright, since a turn's process is its task's. */
            process?: string;
        };
        CardDatesPatch: {
            start?: string;
            end?: string;
            sprint?: string;
        };
        RemoveCardBody: {
            /** @description "unassign", "off-board", or left out for the gesture to decide for itself. Anything else is refused (400). */
            intent?: string;
        };
        PlaceCardBody: {
            /** @description The Monday of the week the card is placed in. */
            week?: string;
        };
        MoveCardBody: {
            /** @description The card to land behind; empty means the top. */
            after?: string;
            /** @description The card to land in front of. It wins when both are sent. */
            before?: string;
        };
        DeferCardBody: {
            /** @description How many days ahead of today the card is pushed; must be positive. */
            days?: number;
        };
        SendToReviewBody: {
            reviewer?: string;
            day?: string;
            /**
             * @description Where the review card stands; empty is the unplanned band, which is where the asking belongs.
             * @enum {string}
             */
            zone?: "" | "urgent" | "unplanned" | "planned" | "niceToHave";
        };
        /** @description A column, named by its EPIC. The project half may be empty — the no-project bucket is a column like any other, and a column's repository is read off the column's own stub. */
        PlacementBody: {
            project?: string;
            epic: string;
        };
        NoteBody: {
            text?: string;
        };
        SprintPatch: {
            team?: string;
            current?: string;
            previous?: string;
            /** @description The repository a NEW team is declared in; an existing team's pointer stays where the team is. */
            domain?: string;
        };
        PersonPatch: {
            /** @description The points a week; 0 takes a set number back. It is required — a body without it is refused (400) rather than read as zero. */
            capacity: number;
        };
        CarryOverBody: {
            team?: string;
            /** @description Report the counts and change nothing. */
            dryRun?: boolean;
        };
        ReorderTeamsBody: {
            teams?: string[];
        };
        TeamBody: {
            team?: string;
        };
        AddEpicBody: {
            name?: string;
            project?: string;
        };
        EpicBody: {
            epic?: string;
            project?: string;
        };
        ReorderEpicsBody: {
            project?: string;
            epics?: string[];
        };
        SetEpicProjectBody: {
            epic?: string;
            /** @description The project the column is leaving. */
            from?: string;
            /** @description The project it joins; empty detaches it. */
            project?: string;
        };
        RenameEpicBody: {
            project?: string;
            epic?: string;
            to?: string;
        };
        AddProcessBody: {
            name?: string;
            project?: string;
            /** @description The repository to declare a project-less process in; one with a project lives with the project. */
            domain?: string;
        };
        ProcessBody: {
            process?: string;
        };
        RenameProcessBody: {
            process?: string;
            to?: string;
        };
        SetProcessProjectBody: {
            process?: string;
            project?: string;
        };
        SetProcessPausedBody: {
            process?: string;
            paused?: boolean;
        };
        ReorderProcessesBody: {
            processes?: string[];
        };
        ReorderProcessTasksBody: {
            process?: string;
            uids?: string[];
        };
        /** @description A task on the wire, for the create (which reads every field) and the patch (which applies only the present ones). */
        TaskRequest: {
            process?: string;
            title?: string;
            description?: string;
            recurrence?: string;
            start?: string;
            team?: string;
            assignee?: string;
            accumulate?: boolean;
        };
        DeadlineBody: {
            week?: string;
            project?: string;
        };
        MoveDeadlineBody: {
            project?: string;
            from?: string;
            to?: string;
        };
        AddProjectBody: {
            name?: string;
            /** @description The repository to declare the project in; empty is the primary. */
            domain?: string;
        };
        ProjectBody: {
            project?: string;
        };
        ReorderProjectsBody: {
            projects?: string[];
        };
        RenameProjectBody: {
            project?: string;
            to?: string;
        };
        RenameTeamBody: {
            team?: string;
            to?: string;
        };
        TeamCapacityBody: {
            team?: string;
            /** @description The points a week; 0 takes the number back. It is required — a body without it is refused (400) rather than read as zero. */
            points: number;
        };
        PresenceBody: {
            /** @description The card the caller has selected; empty clears the mark. */
            card?: string;
        };
        /** @description One frame of the watch stream. No operation references it — a WebSocket upgrade has no response body to describe — but a client applying the stream needs the shapes, so they are stated here. */
        WatchFrame: components["schemas"]["CardFrame"] | components["schemas"]["SprintFrame"] | components["schemas"]["OrderingFrame"] | components["schemas"]["PresenceFrame"] | components["schemas"]["BoardFrame"] | components["schemas"]["LoadFrame"] | components["schemas"]["SyncFrame"] | components["schemas"]["QueueFrame"] | components["schemas"]["SyncErrorFrame"];
        /** @description A card changed. On a SCOPED subscription the verb is about the SELECTION: a card entering it arrives as ADDED and one leaving it as DELETED, whatever became of the card itself. */
        CardFrame: {
            /** @enum {string} */
            type: "ADDED" | "MODIFIED" | "DELETED";
            /** @enum {string} */
            kind: "Card";
            object: components["schemas"]["Card"];
        };
        /** @description A team's sprint pointer moved. It is shaped exactly as the listing shapes it, because a client merges the frame into what it holds: a field the frame left out would be a field the client LOSES. */
        SprintFrame: {
            /** @enum {string} */
            type: "MODIFIED";
            /** @enum {string} */
            kind: "Sprint";
            object: components["schemas"]["Sprint"];
        };
        OrderingFrame: {
            /** @enum {string} */
            type: "MODIFIED";
            /** @enum {string} */
            kind: "Ordering";
            object: components["schemas"]["Ordering"];
        };
        PresenceFrame: {
            /** @enum {string} */
            type: "MODIFIED";
            /** @enum {string} */
            kind: "Presence";
            object: components["schemas"]["Presence"];
        };
        /** @description One person's live selection. */
        Presence: {
            login: string;
            card: string;
        };
        /** @description The board's STRUCTURE changed — a project, a column, a deadline, a process or one of its tasks. It carries the whole board resource plus the process structure, so a client applies it without a round trip, and it is coalesced for a burst of writes. */
        BoardFrame: {
            /** @enum {string} */
            type: "MODIFIED";
            /** @enum {string} */
            kind: "Board";
            /** @description The board resource with the process structure beside it. Stated field by field rather than composed out of Board: a closed schema cannot be extended by an allOf, and the closure is what catches a field added in Go without a line here. */
            object: {
                /** @enum {string} */
                kind: "Board";
                metadata: components["schemas"]["BoardMetadata"];
                processes: components["schemas"]["Process"][];
            };
        };
        /** @description What the board's PEOPLE are holding. Those numbers are summed over cards from every team, so a client holding one view's cards can neither compute them nor learn them from the card events it receives. Shaped as `metadata.members`, to be merged in place. */
        LoadFrame: {
            /** @enum {string} */
            type: "MODIFIED";
            /** @enum {string} */
            kind: "Load";
            object: {
                members: components["schemas"]["Member"][];
            };
        };
        /** @description The board cache was reloaded; the moment it now reflects. */
        SyncFrame: {
            /** @enum {string} */
            type: "MODIFIED";
            /** @enum {string} */
            kind: "Sync";
            object: {
                loadedAt: string;
            };
        };
        /** @description How many writes are still unsynced; the UI hides the number at zero. */
        QueueFrame: {
            /** @enum {string} */
            type: "MODIFIED";
            /** @enum {string} */
            kind: "Queue";
            object: {
                pending: number;
            };
        };
        /** @description A write was lost after its retries. Nobody is spared this one — the client that made the change needs the rollback most of all. */
        SyncErrorFrame: {
            /** @enum {string} */
            type: "ERROR";
            /** @enum {string} */
            kind: "SyncError";
            object: {
                message: string;
            };
        };
    };
    responses: {
        /** @description The request was refused. The status says whose fault it was, `detail` is the sentence to show a person, and `code` is what a program branches on. */
        Problem: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
    };
    parameters: {
        /** @description The card's own address, a ULID assigned at creation. */
        Uid: string;
        /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
        View: string;
        /** @description The note's id inside the card. */
        NoteId: string;
        /** @description The person's forge login. */
        Login: string;
        /** @description A comma-separated set of team keys; the listings union them. Empty is every team, and the no-team group is the empty key. */
        Team: string;
        /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
        Day: string;
        /** @description Whose Me board to read. Who-am-I is resolved server-side, so a caller needs none; an explicit one wins, for a lead looking at somebody else's day. */
        User: string;
        /** @description The Monday the Triage window opens on; empty or absent is the current week. */
        From: string;
        /** @description How many weeks the Triage window covers, that Monday included: 1..26, default 6. Anything else is refused (400). A client must ask for every week it intends to DRAW. */
        Weeks: string;
        /** @description One project's columns, for the Project board; empty is every project, including the columns that belong to none. */
        Project: string;
        /** @description Keep only the cards on one person. */
        Assignee: string;
        /** @description Keep only the cards carrying one stage; present and empty means the cards carrying none. */
        Stage: string;
        /** @description Keep only the cards in one band; present and empty means the cards in none. The filter reads the same answer the listing states, so the two never disagree about a card. */
        Zone: string;
        /** @description "true" or "1" keeps only the cards workable right now — done, on-review and locked are dropped. */
        Focus: string;
        /** @description "true" or "1" brings review cards into the view: on me and team each card's linked review is APPENDED, on triage the reviews the grid otherwise leaves out are KEPT, standing in the week their own dates fall in. Off by default in both. */
        Reviews: string;
        /** @description The resource shape a listing delivers: "summary" (the default, a board row with no description) or "full". Anything else is refused (400). */
        Fields: string;
        /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
        Snapshot: string;
        /** @description Which board is asking, for a read with no board segment of its own: only a DAY board has a day to be a record of, so the snapshot flag is ignored for the others. */
        BoardOfView: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getBoard: {
        parameters: {
            query?: {
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
                /** @description Which board is asking, for a read with no board segment of its own: only a DAY board has a day to be a record of, so the snapshot flag is ignored for the others. */
                view?: components["parameters"]["BoardOfView"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The board. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Board"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    listViews: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The catalog of boards. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ViewList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    listCards: {
        parameters: {
            query?: {
                /** @description A comma-separated set of team keys; the listings union them. Empty is every team, and the no-team group is the empty key. */
                team?: components["parameters"]["Team"];
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description Whose Me board to read. Who-am-I is resolved server-side, so a caller needs none; an explicit one wins, for a lead looking at somebody else's day. */
                user?: components["parameters"]["User"];
                /** @description The Monday the Triage window opens on; empty or absent is the current week. */
                from?: components["parameters"]["From"];
                /** @description How many weeks the Triage window covers, that Monday included: 1..26, default 6. Anything else is refused (400). A client must ask for every week it intends to DRAW. */
                weeks?: components["parameters"]["Weeks"];
                /** @description One project's columns, for the Project board; empty is every project, including the columns that belong to none. */
                project?: components["parameters"]["Project"];
                /** @description Keep only the cards on one person. */
                assignee?: components["parameters"]["Assignee"];
                /** @description Keep only the cards carrying one stage; present and empty means the cards carrying none. */
                stage?: components["parameters"]["Stage"];
                /** @description Keep only the cards in one band; present and empty means the cards in none. The filter reads the same answer the listing states, so the two never disagree about a card. */
                zone?: components["parameters"]["Zone"];
                /** @description "true" or "1" keeps only the cards workable right now — done, on-review and locked are dropped. */
                focus?: components["parameters"]["Focus"];
                /** @description "true" or "1" brings review cards into the view: on me and team each card's linked review is APPENDED, on triage the reviews the grid otherwise leaves out are KEPT, standing in the week their own dates fall in. Off by default in both. */
                reviews?: components["parameters"]["Reviews"];
                /** @description The resource shape a listing delivers: "summary" (the default, a board row with no description) or "full". Anything else is refused (400). */
                fields?: components["parameters"]["Fields"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
            };
            header?: never;
            path: {
                /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
                view: components["parameters"]["View"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description What that board draws. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CardList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    createCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
                view: components["parameters"]["View"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateCardRequest"];
            };
        };
        responses: {
            /** @description The card as it was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    watchView: {
        parameters: {
            query?: {
                /** @description A comma-separated set of team keys; the listings union them. Empty is every team, and the no-team group is the empty key. */
                team?: components["parameters"]["Team"];
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description Whose Me board to read. Who-am-I is resolved server-side, so a caller needs none; an explicit one wins, for a lead looking at somebody else's day. */
                user?: components["parameters"]["User"];
                /** @description The Monday the Triage window opens on; empty or absent is the current week. */
                from?: components["parameters"]["From"];
                /** @description How many weeks the Triage window covers, that Monday included: 1..26, default 6. Anything else is refused (400). A client must ask for every week it intends to DRAW. */
                weeks?: components["parameters"]["Weeks"];
                /** @description One project's columns, for the Project board; empty is every project, including the columns that belong to none. */
                project?: components["parameters"]["Project"];
                /** @description Keep only the cards on one person. */
                assignee?: components["parameters"]["Assignee"];
                /** @description Keep only the cards carrying one stage; present and empty means the cards carrying none. */
                stage?: components["parameters"]["Stage"];
                /** @description Keep only the cards in one band; present and empty means the cards in none. The filter reads the same answer the listing states, so the two never disagree about a card. */
                zone?: components["parameters"]["Zone"];
                /** @description "true" or "1" keeps only the cards workable right now — done, on-review and locked are dropped. */
                focus?: components["parameters"]["Focus"];
                /** @description "true" or "1" brings review cards into the view: on me and team each card's linked review is APPENDED, on triage the reviews the grid otherwise leaves out are KEPT, standing in the week their own dates fall in. Off by default in both. */
                reviews?: components["parameters"]["Reviews"];
                /** @description The resource shape a listing delivers: "summary" (the default, a board row with no description) or "full". Anything else is refused (400). */
                fields?: components["parameters"]["Fields"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
                /** @description A comma-separated set of the kinds to receive; the default is cards,sprints,ordering,presence. */
                resources?: string;
                /** @description The client id changes are attributed to, for echo suppression. */
                client?: string;
            };
            header?: never;
            path: {
                /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
                view: components["parameters"]["View"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Switching to the WebSocket stream. */
            101: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    removeCard: {
        parameters: {
            query?: {
                /** @description A comma-separated set of team keys; the listings union them. Empty is every team, and the no-team group is the empty key. */
                team?: components["parameters"]["Team"];
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description Whose Me board to read. Who-am-I is resolved server-side, so a caller needs none; an explicit one wins, for a lead looking at somebody else's day. */
                user?: components["parameters"]["User"];
                /** @description The Monday the Triage window opens on; empty or absent is the current week. */
                from?: components["parameters"]["From"];
                /** @description How many weeks the Triage window covers, that Monday included: 1..26, default 6. Anything else is refused (400). A client must ask for every week it intends to DRAW. */
                weeks?: components["parameters"]["Weeks"];
                /** @description One project's columns, for the Project board; empty is every project, including the columns that belong to none. */
                project?: components["parameters"]["Project"];
                /** @description Keep only the cards on one person. */
                assignee?: components["parameters"]["Assignee"];
                /** @description Keep only the cards carrying one stage; present and empty means the cards carrying none. */
                stage?: components["parameters"]["Stage"];
                /** @description Keep only the cards in one band; present and empty means the cards in none. The filter reads the same answer the listing states, so the two never disagree about a card. */
                zone?: components["parameters"]["Zone"];
                /** @description "true" or "1" keeps only the cards workable right now — done, on-review and locked are dropped. */
                focus?: components["parameters"]["Focus"];
                /** @description "true" or "1" brings review cards into the view: on me and team each card's linked review is APPENDED, on triage the reviews the grid otherwise leaves out are KEPT, standing in the week their own dates fall in. Off by default in both. */
                reviews?: components["parameters"]["Reviews"];
                /** @description The resource shape a listing delivers: "summary" (the default, a board row with no description) or "full". Anything else is refused (400). */
                fields?: components["parameters"]["Fields"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
            };
            header?: never;
            path: {
                /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
                view: components["parameters"]["View"];
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["RemoveCardBody"];
            };
        };
        responses: {
            /** @description The card is off that board. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    placeCard: {
        parameters: {
            query?: {
                /** @description A comma-separated set of team keys; the listings union them. Empty is every team, and the no-team group is the empty key. */
                team?: components["parameters"]["Team"];
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description Whose Me board to read. Who-am-I is resolved server-side, so a caller needs none; an explicit one wins, for a lead looking at somebody else's day. */
                user?: components["parameters"]["User"];
                /** @description The Monday the Triage window opens on; empty or absent is the current week. */
                from?: components["parameters"]["From"];
                /** @description How many weeks the Triage window covers, that Monday included: 1..26, default 6. Anything else is refused (400). A client must ask for every week it intends to DRAW. */
                weeks?: components["parameters"]["Weeks"];
                /** @description One project's columns, for the Project board; empty is every project, including the columns that belong to none. */
                project?: components["parameters"]["Project"];
                /** @description Keep only the cards on one person. */
                assignee?: components["parameters"]["Assignee"];
                /** @description Keep only the cards carrying one stage; present and empty means the cards carrying none. */
                stage?: components["parameters"]["Stage"];
                /** @description Keep only the cards in one band; present and empty means the cards in none. The filter reads the same answer the listing states, so the two never disagree about a card. */
                zone?: components["parameters"]["Zone"];
                /** @description "true" or "1" keeps only the cards workable right now — done, on-review and locked are dropped. */
                focus?: components["parameters"]["Focus"];
                /** @description "true" or "1" brings review cards into the view: on me and team each card's linked review is APPENDED, on triage the reviews the grid otherwise leaves out are KEPT, standing in the week their own dates fall in. Off by default in both. */
                reviews?: components["parameters"]["Reviews"];
                /** @description The resource shape a listing delivers: "summary" (the default, a board row with no description) or "full". Anything else is refused (400). */
                fields?: components["parameters"]["Fields"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
            };
            header?: never;
            path: {
                /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
                view: components["parameters"]["View"];
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PlaceCardBody"];
            };
        };
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    untriageCard: {
        parameters: {
            query?: {
                /** @description A comma-separated set of team keys; the listings union them. Empty is every team, and the no-team group is the empty key. */
                team?: components["parameters"]["Team"];
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description Whose Me board to read. Who-am-I is resolved server-side, so a caller needs none; an explicit one wins, for a lead looking at somebody else's day. */
                user?: components["parameters"]["User"];
                /** @description The Monday the Triage window opens on; empty or absent is the current week. */
                from?: components["parameters"]["From"];
                /** @description How many weeks the Triage window covers, that Monday included: 1..26, default 6. Anything else is refused (400). A client must ask for every week it intends to DRAW. */
                weeks?: components["parameters"]["Weeks"];
                /** @description One project's columns, for the Project board; empty is every project, including the columns that belong to none. */
                project?: components["parameters"]["Project"];
                /** @description Keep only the cards on one person. */
                assignee?: components["parameters"]["Assignee"];
                /** @description Keep only the cards carrying one stage; present and empty means the cards carrying none. */
                stage?: components["parameters"]["Stage"];
                /** @description Keep only the cards in one band; present and empty means the cards in none. The filter reads the same answer the listing states, so the two never disagree about a card. */
                zone?: components["parameters"]["Zone"];
                /** @description "true" or "1" keeps only the cards workable right now — done, on-review and locked are dropped. */
                focus?: components["parameters"]["Focus"];
                /** @description "true" or "1" brings review cards into the view: on me and team each card's linked review is APPENDED, on triage the reviews the grid otherwise leaves out are KEPT, standing in the week their own dates fall in. Off by default in both. */
                reviews?: components["parameters"]["Reviews"];
                /** @description The resource shape a listing delivers: "summary" (the default, a board row with no description) or "full". Anything else is refused (400). */
                fields?: components["parameters"]["Fields"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
            };
            header?: never;
            path: {
                /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
                view: components["parameters"]["View"];
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    finishedEarlier: {
        parameters: {
            query?: {
                /** @description A comma-separated set of team keys; the listings union them. Empty is every team, and the no-team group is the empty key. */
                team?: components["parameters"]["Team"];
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description Whose Me board to read. Who-am-I is resolved server-side, so a caller needs none; an explicit one wins, for a lead looking at somebody else's day. */
                user?: components["parameters"]["User"];
                /** @description The Monday the Triage window opens on; empty or absent is the current week. */
                from?: components["parameters"]["From"];
                /** @description How many weeks the Triage window covers, that Monday included: 1..26, default 6. Anything else is refused (400). A client must ask for every week it intends to DRAW. */
                weeks?: components["parameters"]["Weeks"];
                /** @description One project's columns, for the Project board; empty is every project, including the columns that belong to none. */
                project?: components["parameters"]["Project"];
                /** @description Keep only the cards on one person. */
                assignee?: components["parameters"]["Assignee"];
                /** @description Keep only the cards carrying one stage; present and empty means the cards carrying none. */
                stage?: components["parameters"]["Stage"];
                /** @description Keep only the cards in one band; present and empty means the cards in none. The filter reads the same answer the listing states, so the two never disagree about a card. */
                zone?: components["parameters"]["Zone"];
                /** @description "true" or "1" keeps only the cards workable right now — done, on-review and locked are dropped. */
                focus?: components["parameters"]["Focus"];
                /** @description "true" or "1" brings review cards into the view: on me and team each card's linked review is APPENDED, on triage the reviews the grid otherwise leaves out are KEPT, standing in the week their own dates fall in. Off by default in both. */
                reviews?: components["parameters"]["Reviews"];
                /** @description The resource shape a listing delivers: "summary" (the default, a board row with no description) or "full". Anything else is refused (400). */
                fields?: components["parameters"]["Fields"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
            };
            header?: never;
            path: {
                /** @description The board the caller is standing on: me, team, triage, backlog, project, or all — the escape hatch, which has to be said out loud. A board nobody has is a route that is not there (404). */
                view: components["parameters"]["View"];
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    getCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card is gone. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    patchCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CardPatch"];
            };
        };
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    moveCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MoveCardBody"];
            };
        };
        responses: {
            /** @description The order is applied. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    deferCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DeferCardBody"];
            };
        };
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    setInProgress: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    reopenCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    sendToReview: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SendToReviewBody"];
            };
        };
        responses: {
            /** @description The review card. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    mirrorCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PlacementBody"];
            };
        };
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    unmirrorCard: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PlacementBody"];
            };
        };
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    removeFromProject: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PlacementBody"];
            };
        };
        responses: {
            /** @description The card is out of that column. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    removeReviewer: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card after the change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Card"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    listLinks: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card's links. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LinkList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    getCardLog: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The card's feed. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LogList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    getDayLogs: {
        parameters: {
            query?: {
                /** @description The board day to read, yyyy-mm-dd. Empty or absent is today. */
                day?: string;
                /** @description The cards to read, comma-separated; at most 200 per request. */
                uids?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description That day, per card. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DayLogList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    listNotes: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The thread. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NoteList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    addNote: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["NoteBody"];
            };
        };
        responses: {
            /** @description The thread with the new note in it. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NoteList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteNote: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
                /** @description The note's id inside the card. */
                noteId: components["parameters"]["NoteId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The thread the note has left. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NoteList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    editNote: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
                /** @description The note's id inside the card. */
                noteId: components["parameters"]["NoteId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["NoteBody"];
            };
        };
        responses: {
            /** @description The thread after the edit. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NoteList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    listSprints: {
        parameters: {
            query?: {
                /** @description The day a day board is read for, yyyy-mm-dd. Empty or absent is today. */
                day?: components["parameters"]["Day"];
                /** @description "true" or "1", with a PAST day on a me or team board: the board as it STOOD when that day ended, rather than today's cards filtered by that day's dates. Whether a day is over is asked per team. A day the clone's history no longer reaches is 410, and storage that keeps no history answers 501. Ignored where a board has no records to give. */
                snapshot?: components["parameters"]["Snapshot"];
                /** @description Which board is asking, for a read with no board segment of its own: only a DAY board has a day to be a record of, so the snapshot flag is ignored for the others. */
                view?: components["parameters"]["BoardOfView"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The pointers. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SprintList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    patchSprint: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SprintPatch"];
            };
        };
        responses: {
            /** @description The pointer as it now stands. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Sprint"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    patchPerson: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The person's forge login. */
                login: components["parameters"]["Login"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PersonPatch"];
            };
        };
        responses: {
            /** @description The board, with the person's new number in it. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Board"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    carryOver: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CarryOverBody"];
            };
        };
        responses: {
            /** @description What was carried. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CarryReport"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    reorderTeams: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReorderTeamsBody"];
            };
        };
        responses: {
            /** @description The order is applied. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteTeam: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TeamBody"];
            };
        };
        responses: {
            /** @description The team is gone from the roster. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    addEpic: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AddEpicBody"];
            };
        };
        responses: {
            /** @description The column is declared. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteEpic: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["EpicBody"];
            };
        };
        responses: {
            /** @description The column is gone. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    reorderEpics: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReorderEpicsBody"];
            };
        };
        responses: {
            /** @description The order is applied. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    setEpicProject: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetEpicProjectBody"];
            };
        };
        responses: {
            /** @description The column belongs to the other project now. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    renameEpic: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RenameEpicBody"];
            };
        };
        responses: {
            /** @description The column is renamed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    listProcesses: {
        parameters: {
            query?: {
                /** @description Keep only the processes of one project; empty is every project. */
                project?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The processes. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProcessList"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    addProcess: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AddProcessBody"];
            };
        };
        responses: {
            /** @description The process is declared. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteProcess: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProcessBody"];
            };
        };
        responses: {
            /** @description The process is gone. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    renameProcess: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RenameProcessBody"];
            };
        };
        responses: {
            /** @description The process is renamed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    setProcessProject: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetProcessProjectBody"];
            };
        };
        responses: {
            /** @description The process belongs to the other project now. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    setProcessPaused: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetProcessPausedBody"];
            };
        };
        responses: {
            /** @description The process is paused, or running again. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    reorderProcesses: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReorderProcessesBody"];
            };
        };
        responses: {
            /** @description The order is applied. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    reorderProcessTasks: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReorderProcessTasksBody"];
            };
        };
        responses: {
            /** @description The order is applied. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    addTask: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TaskRequest"];
            };
        };
        responses: {
            /** @description The uid of the new task. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TaskRef"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteTask: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The task is gone. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    patchTask: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The card's own address, a ULID assigned at creation. */
                uid: components["parameters"]["Uid"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TaskRequest"];
            };
        };
        responses: {
            /** @description The task is changed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    addDeadline: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DeadlineBody"];
            };
        };
        responses: {
            /** @description The line is drawn. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteDeadline: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DeadlineBody"];
            };
        };
        responses: {
            /** @description The line is gone. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    moveDeadline: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MoveDeadlineBody"];
            };
        };
        responses: {
            /** @description The line has moved. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    addProject: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AddProjectBody"];
            };
        };
        responses: {
            /** @description The project is declared. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    deleteProject: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProjectBody"];
            };
        };
        responses: {
            /** @description The project is gone. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    reorderProjects: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReorderProjectsBody"];
            };
        };
        responses: {
            /** @description The order is applied. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    renameProject: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RenameProjectBody"];
            };
        };
        responses: {
            /** @description The project is renamed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    renameTeam: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RenameTeamBody"];
            };
        };
        responses: {
            /** @description The team is renamed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    setTeamCapacity: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TeamCapacityBody"];
            };
        };
        responses: {
            /** @description The number is recorded. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    setPresence: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PresenceBody"];
            };
        };
        responses: {
            /** @description The selection is shared. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            default: components["responses"]["Problem"];
        };
    };
    getOpenAPI: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The OpenAPI description of this surface. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": Record<string, never>;
                };
            };
            default: components["responses"]["Problem"];
        };
    };
}
