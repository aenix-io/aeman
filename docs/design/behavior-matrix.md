# Behavior test matrix — current logic, pinned before the API redesign

Companion to [api-redesign.md](api-redesign.md). Every rule the board runs on
today, where it lives, and the test that pins it. Rules marked **frontend** are
being moved server-side by the redesign — their tests are written FIRST against
the new service methods and must encode today's frontend behaviour exactly.
The date logic is the fragile part; it gets the densest coverage.

Legend: ✅ existing Go test · 🆕 new test written for the redesign ·
(file names are relative to `internal/`)

## Dates & visibility

| # | Rule (as shipped today) | Lives in | Test |
| --- | --- | --- | --- |
| D1 | Two dates: `startDate` (scheduled day) + `sprintStart` (sprint membership); `day` = end of the visible range | board model | ✅ board/filters_test, board_test |
| D2 | Team grid: a materialized card shows on its sprint day AND its own start day | board.TeamGrid | ✅ filters_test `a sprint day keeps all its cards…`, `a later-created card…` |
| D3 | Team grid: a card with `end >= start` shows on **every** day of `[start, end]` | board.TeamGrid | ✅ filters_test `a ranged card…` (4 cases) |
| D4 | Team grid: deferred card (`start > today`) shows on its own day (or range) + its past sprint day; hidden elsewhere | board.TeamGrid | ✅ filters_test `deferred…` (3 cases) |
| D5 | Team grid: passed-through history — pointer day S (current/previous) with `origin ≤ S < sprintStart` keeps carried/demoted cards | board.TeamGrid | ✅ filters_test `a sprint day keeps all its cards…` |
| D6 | Me view: `activeSprint(day) ≤ sprintStart`, gated by `start ≤ day`; deferred hidden until its day; ranges span | board.MeView | ✅ filters_test TestMeView |
| D7 | activeSprint = current if `day ≥ current`, else previous if `day ≥ previous`, else "" | board.ActiveSprint | ✅ sprint_test |
| D8 | Create defaults: start/day default to each other (one-day range), never today for backdated creates; sprint = team current; first sprint recorded; plan cards get no dates | boardservice.CreateCard | ✅ service_test (create suite) |
| D9 | **Defer +N: target = `max(today, start) + N`; only `start` moves; sprint keeps the card's history day** | **frontend** (Card.moveStart + handleDefer) | 🆕 service_test `TestDefer*` |
| D10 | **Defer of a same-day card (createdAt today): full relocation — sprint and a stale end date move along** | **frontend** (handleDefer) | 🆕 service_test `TestDeferSameDay*` |
| D11 | **Calendar set-dates: `start` → startDate, sprint = `activeSprint(team, start)` else start, `end` → day** | **frontend** (handleSetDates) | 🆕 service_test `TestSetDates*` |
| D12 | Carry over: only the closing sprint's unfinished cards move; demoted/old cards stay (removal is final) | boardservice.CarryOver | ✅ service_test carry suite |
| D13 | Carry over: finished recurrent reseeds a fresh copy (title/description/assignee/zone, 0%, no notes) | boardservice.CarryOver | ✅ `TestCarryOverReseedsFinishedRecurrent` |
| D14 | Carry week: same for plan cards + same-title dedup (idempotent) | boardservice.CarryWeek | ✅ `TestCarryWeekReseedsFinishedRecurrent` |
| D15 | Carry over is idempotent for today; advances pointer even with nothing to carry | boardservice.CarryOver | ✅ service_test |
| D16 | **Carry over / carry week dry run: report would-be counts, mutate nothing** (backs the UI confirm) | **frontend** (confirm-count filters) | 🆕 service_test `TestCarryOverDryRun`, `TestCarryWeekDryRun` |

## Status & progress

| # | Rule | Lives in | Test |
| --- | --- | --- | --- |
| S1 | review/locked clamp progress to [10, 90] | board.ClampProgress | ✅ status_test |
| S2 | Done is derived: 100% + no stage; never stored; picking Done clears stage + fills 100 | board.ApplyProgress/ApplyStage | ✅ status_test, service_test, server/api_test |
| S3 | Complete = done ∨ (100% ∧ (none ∨ recurrent)); review/locked@100 unfinished | board.Complete | ✅ status_test TestComplete |
| S4 | Recurrent: unclamped 0–100; excluded from plan progress | board + view | ✅ status via S3; 🆕 view test `TestWeeklyViewProgressExcludesRecurrent` |
| S5 | In Progress: clears stage, nudges into [10,90] at the edges | board.ApplyInProgress | ✅ status_test |
| S6 | Review card progress drives the original's review stage (100 → off review, <100 → back on) | boardservice.syncReviewLink | ✅ service_test review suite |
| S7 | review/locked store the [10, 90] clamp on stage pick (0 → 10, 100 → 90), so the stored value matches the band | board.ApplyStage | ✅ status_test |

## Actions (the frontend logic moving server-side)

| # | Rule | Lives in | Test |
| --- | --- | --- | --- |
| A1 | **Smart remove (grid): in the team's current sprint → demote (start=sprint=previous, end pulled along); otherwise the card is handed back — assignee + sprint cleared, and a card with no Project-board column and no plan band lands in this week's plan. The grid × NEVER deletes: taking a card off a person does not destroy the work, and a card created and assigned the same day used to vanish here** | **frontend** (handleGridDelete) | 🆕 service_test `TestRemoveGrid*` |
| A2 | **Smart remove (grid, plan-taken card): clear assignee + sprint — back to plan-only, keeps plan marker** | **frontend** (handleGridDelete plan branch) | 🆕 `TestRemoveGridPlanTaken` |
| A3 | **Smart remove (plan band): a card filed under a project (either side of the (project, epic) pair) only leaves the plan — never deleted; an assigned-or-worked card keeps working and loses just plan+week; a pure plan card (no column, unassigned, never worked) is deleted for real** | **frontend** (removeFromPlan) | 🆕 `TestRemovePlan*` |
| A4 | Real delete cascades to the linked review card | boardservice.DeleteCard | ✅ service_test |
| A5 | **Leaving review (stage change / in-progress) cancels the unfinished linked review card: demote + break the reviewOf link, or delete; a finished review card stays** | **frontend** (cancelLinkedReview) | 🆕 `TestStageOffReviewCancelsLinked*` |
| A6 | Send-to-review: creates the linked review card (reviewer, yellow, viewed day), original → review stage | boardservice.SendToReview | ✅ service_test |
| A7 | Reassign / remove reviewer | boardservice | ✅ service_test |
| A8 | Take-into-plan / release-from-plan | boardservice | ✅ service_test |
| A9 | Move: reorder after a card / to the top; order is board-level | backend MoveCard + store | 🆕 apiserver `TestOrdering*` (uid list + move) |

## Views & watch (resource layer)

| # | Rule | Lives in | Test |
| --- | --- | --- | --- |
| V1 | Team/Me/Weekly LIST selectors return exactly what the UI renders, sorted by board order | board filters (✅) + 🆕 apiserver `TestListSelectors*` |
| V2 | Weekly view response carries wed/fri bands + plan progress (recurrent excluded, done→100, average) | **frontend** (planProgress) | 🆕 `TestWeeklyViewProgress*` |
| W1 | Weekly history: a worked plan card (has a start date) carried forward keeps showing in every past week it was worked in, mirroring the day grid's sprint history; a pure never-started plan card moves with its week | board.planShowsInWeek + TeamBoard showsInWeek | ✅ TestWeeklyHistoryForWorkedCards |
| W2 | Smart-remove never deletes nor unhooks a worked card: the plan × on an assigned-or-worked card and the grid × on a worked taken card shed only the plan membership (person, dates and sprint history stay); the ONLY × that still deletes is the plan × on a pure plan card — no Project-board column, unassigned, never worked — because the old previous-week demote boomeranged back on the next carry-week. Everything else is handed back: the grid × releases the card to the weekly plan instead of deleting it | boardservice.Remove/ReleaseFromPlan/releaseToPlan | ✅ TestPlanRemoveKeepsWorkedCard / TestGridRemoveOnTakenPlanCard / TestRemoveGridReleasesProjectCardInsteadOfDeleting |
| W4 | A Project-board slot needs no stored band to be on the Weekly panel: its span is its plan (it shows on every week between its boundaries), and its band derives from the end date — by-Wednesday only in the week its end falls in, by-Friday in every earlier covered week. A stored band always outranks the derived one, so deriving never moves a hand-placed card. The Me/Team plan stripe shows the same effective band | board.WeeklyPlanAt + TeamBoard/Card mirror (weekly.ts) | ✅ TestWeeklyPlanDerivesSlotBands / weekly.test.ts |
| W3 | Carried plan cards tighten: a by-Friday card carried into the next week lands in its by-Wednesday band (already overdue); by-Wednesday stays; reseeded recurrents keep their band. Week-history entries render in the past week's by-Friday band (they stayed open through that week's end) | boardservice.CarryWeek + board.WeeklyPlan + TeamBoard mirror | ✅ TestCarryWeekTightensFriToWed / TestWeeklyHistoryLandsInFriBand |
| L5 | Activity log: every aeman mutation (create, stage, progress, assignee, team, zone, review sent/passed/removed/round-reset, plan taken/released, dates, sprint, plan week/band) records an event with the acting user. The event IS the commit: the field diff says what changed, an `Aeman-Change` trailer says what the diff cannot (a reviewer named, a round reset); the log is read back from the history within the horizon (`truncatedBefore` when cut), so a direct git write shows up like any other change and nothing is stored on the card. GET /cards/{uid}/log + MCP list_log serve events+notes as one chronological feed; the Me day panel and CardDetail render it. Carries log the same kinds as manual moves (sprint/week/plan-band) and reseeds log created; a plan join/leave logs plan-added/plan-released | board.Event + boardservice.logEvent + gitstore commits (Backend.CardLog) | ✅ TestMutationsRecordEvents / TestReviewCycleRecordsEvents / TestAPICardLogUnifiedFeed / gitstore TestCardLogReadsTrailersFirstThenDiff / server TestCardLogFromCommits |
| V3 | Watch (unscoped): ADDED/MODIFIED/DELETED per card; echo suppression by client id | server store | ✅ (live-verified) + 🆕 `TestWatchEvents*` |
| V4 | Watch (view-scoped): entering/leaving the selection delivers ADDED/DELETED for that subscription | 🆕 apiserver `TestScopedWatch*` |
| V5 | Ordering singleton: move emits one MODIFIED Ordering; LIST stays sorted | 🆕 apiserver `TestOrdering*` |

| V+ | Echo suppression is scoped to the ADDRESSED card: the author's watch is spared the echo only for the {uid} their request named — a batch fan-out (epic rename over its cards) or a cascade (a subtask following its parent) echoes even to the author, who holds no optimistic copy of those cards. Unscoped suppression made a renamed column's cards vanish from the very board that renamed it | internal/server echoOrigin + clientIDMiddleware | ✅ TestBatchEchoesReachTheirAuthor / TestAddressedCardStaysSuppressed |

| R+ | A review card's own STAGE drives its original exactly as its progress does: marking the review card done passes the review (the original leaves the review stage, a review-passed event names the reviewer), and lowering it below 100 — Reopen — sends the original back on review. Clearing the stage of a card at 100 changes nothing: it is complete whatever the stage says. Only the progress paths synced this, so "mark as done" on a review card left the original stuck on review with nothing in its log | boardservice.SetStage → syncReviewLink | ✅ TestReviewDoneByStagePassesTheOriginal / TestReopeningAReviewCardReturnsTheOriginal |

| S+ | A subtask left BEHIND in an earlier sprint still renders under its parent: a completed subtask stays in the sprint it was finished in while the parent carries on, and the parent's progress bar is derived from exactly those children. Hiding them (the old `activeSprint > sprintStart` rule) left a card handed to someone else reading 90% with no expand arrow and subtasks its new owner could see named in the log but could not open. Only a subtask whose day has not arrived — deferred ahead, or a sprint that had not started on the viewed day — stays hidden | web/src/subtasks.ts (shared by MeBoard and TeamBoard) | ✅ subtasks.test.ts |

| S2 | Ungrouping hands an OWNERLESS child the parent's person: a subtask usually has no assignee of its own (it rides the parent's), so a pull-out left it in Unassigned and off every personal board — from the person who pulled it, the card vanished. A patch that also names assignees wins, including an explicit empty one: assignees are applied AFTER the parent so a deliberate drop into Unassigned lands | boardservice.SetParent + handlePatchCard ordering | ✅ TestUngroupGivesTheParentsPerson / TestUngroupKeepsItsOwnPerson / TestPatchUngroupRespectsExplicitAssignees |
| W5 | The × on a worked card ASKS instead of deciding: a card in the current sprint with a previous one to fall back to, not created today, and carrying progress opens a two-way choice — keep it in the previous sprint (the old silent behaviour, subtasks riding along) or delete it outright. An untouched card still demotes silently, and everything else still deletes. The browser confirm is suppressed when the board asks, since a prompt reading "Delete?" in front of a demote is how the × came to be read as deletion | web/src/removal.ts + RemoveChoiceDialog, TeamBoard | ✅ removal.test.ts |
| W6 | The demote RECORDS its move (`sprint` event, from → to). It wrote start, sprint and day silently while the subtasks it dragged logged theirs, so a card left the board with nothing in its own history to explain it — twice the cause of a misread incident | boardservice.Remove | ✅ TestDemoteRecordsItsMove |

| S3 | A subtask's PERSON always follows its parent, like its team: grouping hands the child to the parent's assignee, a direct change on a subtask snaps back, and re-assigning (or unassigning) a parent cascades to every subtask. Without it a split family lands on two personal boards, since the Me view admits a card when you own one of its subtasks — one stray child dragged the parent and all its siblings onto a stranger's board | boardservice.SetAssignee + SetParent, mirrored in TeamBoard | ✅ TestSubtaskOwnerFollowsTheParent |

## Card object mapping

| # | Rule | Test |
| --- | --- | --- |
| M1 | Domain card ↔ resource Card (metadata/spec/status) round-trips every field; zones map to semantic names (urgent/unplanned/planned/niceToHave ↔ red/yellow/gray/green) | 🆕 apiserver `TestCardResource*` |
| M2 | `status.complete/inProgress/reviewedBy` derived correctly | 🆕 apiserver `TestCardStatus*` |
| M3 | Notes as a separate collection: a `## Notes` section of the card file in the git store; the migration's reader still parses the Projects v2 draft-body log | ✅ gitstore TestCardFileNotesFormat / TestCardFileDescriptionMayMentionNotes; ghsource TestMapDomainItemContent |

The 🆕 tests are written and committed **red-first** (or against thin stubs)
before the implementation lands, so the port from the frontend cannot silently
drift from today's behaviour.

## Old → new coverage audit

Every surface of the pre-redesign API, checked off against its replacement.
Done after the cutover so nothing silently dropped.

### HTTP endpoints (35 old → new)

| Old | New | Notes |
| --- | --- | --- |
| GET /board | GET /board | Field metadata dropped by design — zones are semantic now, no option-id mapping for clients. |
| GET /snapshot | GET /cards + /sprints | Split per resource; board order preserved in the list. |
| GET /watch | GET /watch | New frames {type, kind, object}; RELOAD replaced by Sprint/Ordering events + scoped re-diffs. |
| GET /team, /me, /weekly | GET /cards?view= | Weekly bands via spec.plan.band; +weekly.progress. |
| POST /cards | POST /cards | Body follows the spec shape. |
| POST /carry-over, /carry-week | POST /sprints/actions/… | +dryRun count reports. |
| POST /sprint-state | PATCH /sprints | |
| DELETE /cards/{id} | DELETE /cards/{uid} | Cascade unchanged. |
| …/stage | PATCH {stage} | + server-side review-cancel cascade (was frontend). |
| …/in-progress | …/actions/in-progress | |
| …/progress | PATCH {progress} | Done-link unchanged. |
| …/zone | PATCH {zone} | Semantic names; colour keys rejected. |
| …/day | PATCH {dates:{end}} | |
| …/start | PATCH {dates:{start}} | Now runs the calendar rule (sprint follows). Granular start-only writes had no standalone use: defer and demote are actions. |
| …/sprint-start | PATCH {dates:{sprint}} | |
| …/plan, …/week | PATCH {plan:{band, week}} | |
| …/assignee | PATCH {assignees} | |
| …/team | PATCH {team} | Sprint join falls back to today (was: the UI's selected day). Deliberate: the server has no view state. |
| …/take-plan, …/release-plan | …/actions/take-into-plan, release-from-plan | |
| …/move | …/actions/move | + Ordering resource/events. |
| …/note, …/notes/{id} | …/notes CRUD | Mutations return the NoteList. |
| …/description | PATCH {description} | |
| …/rename | PATCH {title} | |
| …/review | …/actions/send-to-review | |
| …/review/reassign | (folded) | send-to-review reassigns when a review card exists. |
| …/review/remove | …/actions/remove-reviewer | |
| — | …/actions/remove | New: the frontend's smart × (A1–A3). |
| — | …/actions/defer | New: D9/D10. |
| — | PATCH {reviewOf} | New: the review link is writable (A5 fix). |
| — | GET /ordering | New singleton. |

### MCP tools (21 old → 20 new)

get_board ✓ · team_view/me_view/weekly_plan → list_cards ✓ · create_card ✓ ·
carry_over/carry_week ✓ (+dryRun) · set_stage/set_progress/set_assignee/
set_team/rename_card → update_card ✓ · set_in_progress → **in_progress**
(added by this audit — update_card cannot express the nudge) ·
send_to_review ✓ · reassign_reviewer → folded into send_to_review ✓ ·
remove_reviewer ✓ · take_into_plan/release_from_plan ✓ ·
move_card/delete_card/add_note ✓ · new: get_card, list_notes, edit_note,
delete_note, remove_card, defer_card, update_card(dates/plan/reviewOf).

### Frontend logic moved server-side

Defer (D9/D10) · calendar set-dates (D11) · smart remove (A1–A3) ·
review-cancel cascade incl. the reviewOf break (A5) · first-sprint record on
create (D8, was doubled client-side) · carry confirm counts (D16) · weekly
plan progress (V2). Each is pinned by the 🆕 tests above.

## Links (2026-07-02)

| # | Rule | Lives in | Test |
| --- | --- | --- | --- |
| L1 | Description links: GitHub issue/PR refs first (resolved to titles via the backend resolver; unresolvable ones stay unresolved, not dropped), plain links after, deduped | board.ExtractLinks + boardservice.CardLinks | ✅ links_test, service_test TestCardLinks, server TestAPICardLinks |
| L2 | Create-by-URL: a title that is only a GitHub issue/PR URL resolves to that item's title, the link moves into the description; unresolvable keeps the URL title | boardservice.CreateCard | ✅ TestCreateCardFromGitHubURL / FromURLUnresolved |
| L3 | UI: hashtag icon (link icon when only plain links) before the stage icon; one menu, refs-with-titles first, plain links as-is; click opens a new tab | Card.tsx + links.ts mirror | manual |
| L4 | Send-to-review copies the description; afterwards it LIVE-SYNCS across the review link both ways (notes stay per-card) | boardservice.SendToReview / SetDescription | ✅ TestSendToReviewCopiesDescription, TestSetDescriptionSyncsAcrossReviewLink |

## Review-card lifecycle (2026-07-02)

| # | Rule | Lives in | Test |
| --- | --- | --- | --- |
| R1 | A review card the reviewer worked on (progress > 0) is never auto-removed: leaving review / done keeps it untouched, link intact | boardservice.cancelLinkedReview | ✅ TestStageOffReviewKeepsWorkedReviewCard |
| R2 | Reassigning a reviewer who worked keeps their card (released from the link, stays behind on the next carry) and spawns a fresh review card; an untouched card is handed over in place | boardservice.ReassignReviewer | ✅ TestReassignWorkedReviewerSpawnsNewCard / InPlace |
| R3 | Carry-over moves a review card only while the review is still required: original unfinished on the review stage + a reviewer assigned; stale review work stays behind | boardservice.CarryOver | ✅ TestCarryOverReviewCardsOnlyWhileRequired |

## Focus filter (2026-07-03)

| # | Rule | Lives in | Test |
| --- | --- | --- | --- |
| F1 | Workable = not done, not on-review, not locked (keeps in-progress, not-started, recurrent<100) | board.Workable | ✅ apiserver TestSelectorFocusAndMultiTeam |
| F2 | `focus=true` selector keeps only workable cards; shared by the Me view toggle, the HTTP LIST and MCP list_cards | apiserver.Selector.Focus + FilterCards | ✅ TestSelectorFocusAndMultiTeam |
| F3 | On view=me / default, `team=` filters by team as a comma-separated set (the eye toggle over the selected chips) | apiserver.FilterCards teamInSet | ✅ TestSelectorFocusAndMultiTeam |
| R4 | Re-review reactivates the completed review card whenever a passed original is put back on review — the stage menu (SetStage→review) or re-sending to the SAME reviewer both trigger it: review card progress→0, round counter ticks up (round 1 implicit, first re-review = 2), original on review. Symmetric reverse of the review-done→off-review forward sync; a still-in-progress review or a different reviewer is untouched | boardservice.reactivateReviewCard (SetStage + ReassignReviewer) | ✅ TestReReviewReactivatesReviewCard / ViaStageMenu / DifferentReviewer / EnterReviewNoCompletedCardIsNoop |
| R5 | A review card is created in the same sprint as the card it reviews (original's sprintStart), not the team's current pointer | boardservice.sendToReview | ✅ TestSendToReviewUsesOriginalSprint |
| R6 | Carry-over leaves a completed review card behind, pinned to the closing sprint's day, so a card created today does not linger on the new sprint via its own start-day | boardservice.CarryOver | ✅ TestCarryOverPinsCompletedReviewCard |
| R7 | Re-review pulls the review card into the original's current sprint and onto today (reappears in the new sprint) and bumps the round | boardservice.reactivateReviewCard | ✅ TestReReviewRelocatesToNewSprintWithCounter |
| V1 | The Team board loads its day grid PLUS the weekly plan of the shown teams (view=weekly accepts a comma set); the plan panel renders from the same card set | apiserver weekly multi-team + App dual fetch | ✅ TestWeeklyViewMultiTeam / viewquery.test |
| V2 | GET /board carries the people roster (members: every distinct assignee), so assign/review/view-as pickers work with per-view card loading; Me view-as sends the impersonated user explicitly | apiserver BoardResource + App viewAs | ✅ TestBoardResourceMembers / viewquery.test |

## Storage: git backend (2026-08-27)

Companion to [git-backend.md](git-backend.md). These rules were pinned BEFORE the backend existed: each test was written first in the package that owns the rule (`pkg/gitstore`, `pkg/board`, `pkg/boardservice`, `internal/server`, `cmd/aeman`, the migration, `pkg/apiserver`) and failed before the code landed; rows G27+ were added the same way as the cut-over exposed rules the design had left implicit. Shallow-path tests (G9–G11) run against the real `git-upload-pack`/`git-receive-pack` binaries (go-git's in-process server has no shallow support); they skip locally when the binaries are absent and fail in CI (`AEMAN_TEST_REQUIRE_GIT=1`). G8 is hermetic via `SetShallow`. Rules about dates, sprints, visibility, review and carry-over do not change and keep their rows above.

| # | Rule | Lives in | Test |
| --- | --- | --- | --- |
| G1 | Card path = `cards/<a>/<b>/<id>.md`, `a`,`b` = the id's LAST two chars; the path never changes while the card exists in a domain (rename, move, re-zone, re-team keep it) | gitstore layout | gitstore `TestCardPathShardsByTail`, `TestCardPathTailDecidesTheLeaf`, `TestCardPathNormalisesAndRefuses`, `TestBackendSettersRewriteOnlyTheCard`, `TestBackendMoveCardRewritesOneFile` |
| G2 | Empty fields are omitted on write; unknown front-matter keys survive a rewrite | gitstore file format | gitstore `TestCardFileRoundTrip`, `TestCardFileOmitsEmptyFields`, `TestCardFileClearedFieldIsOmitted`, `TestCardFileKeepsUnknownKeys`, `TestRosterKeepsUnknownKeys` |
| G3 | Derived states are never written: 100% writes `progress: 100` and `doneFrom` (a review passing 90 → 100 writes `doneFrom: 90`), never `stage: done`; In Progress is absent from every file | gitstore file format | gitstore `TestCardFileDerivedStateNotStored`, `TestServiceDoneAndReopenThroughTheBackend` |
| G4 | One action = one commit per touched domain sharing `Aeman-Action-Id`; `Aeman-Cards` lists every id; carry-over with zero cards still commits the advanced pointer (team file only); a team already on today's sprint makes no commit | gitstore commit | gitstore `TestCommitTouchesEveryFileOnce`, `TestCommitNoopMakesNoCommit`, `TestBackendScopeCommitsOnce`, `TestMultiBackendRefilingIsAMove`, `TestServiceCarryOverThroughTheBackend`; server `TestGitOneRequestOneCommit`, `TestGitTwoDomainsMovePushesBothAndIsAdopted`; boardservice `TestCarryOverWithNothingToCarryStillAdvances`, `TestCarryOverIdempotentWhenAlreadyToday` |
| G5 | Coalesced field writes commit once with the final value, keyed by `(op, card, actor)`; two actors on one slider → two attributed commits; an action on a card flushes its pending coalesced writes first (per-card FIFO) | server write queue | server `TestGitProgressCoalescesByActor`, `TestGitActionFlushesCoalescedFirst` |
| G6 | Author = actor login (email from template), committer = server identity, date = action time; the sweep, title resolution, import and schema migration are authored `aeman` | gitstore commit | gitstore `TestCommitIdentity`, `TestCommitUnattributedIsTheServer`, `TestUnattributedWriteIsAuthoredByTheServer`; server `TestSweepOnFetchTickFilesThisWeeksTurnsOnce`, `TestGitModeMigratesOlderSchemaAtStartup`; migrate `TestEventsBecomeCommits` |
| G41 | The day boards ask ONE question — what happened on this day, across the cards on screen — and get one answer: `GET /logs?day&uids` returns each card's notes and events of that day, the walk stopping at the day's first moment (`CardLogSince`), a quiet card present and empty, an unreadable card absent. A card's own pane keeps the other question (its whole history). Asking a full log per card is what made opening a board fire dozens of second-long requests on a migrated history | gitstore.CardLogSince + boardservice.DayLogs + `GET /logs` | gitstore `TestCardLogSinceStopsAtTheBoundary`; boardservice `TestDayLogsAnswersManyCardsForOneDay`, `TestDayLogsDefaultsAndEmptyInput`; server `TestDayLogsAnswersForTheVisibleCardsOnly` |
| G7 | A card's log = commits touching it within the horizon, across `Aeman-Moved-From`; from/to from an `Aeman-Change` trailer first, the front-matter diff second — a `created`/`deleted` the trailer already states is one event, not two; a root commit (the import) belongs only to the cards in its tree; `truncatedBefore` set when the horizon cuts it. The candidates come from a per-path index built by one walk — `Aeman-Cards` names a commit's cards without reading a tree, a commit without the trailer is diffed against its parent — extended as the head grows, rebuilt after a reset or a moved shallow boundary (20k commits: ~1 s once, then a map read per log instead of seconds) | gitstore history | gitstore `TestCardLogReadsTrailersFirstThenDiff`, `TestCardLogCreatedAndDeletedOnceWhenTheTrailerSaysSoToo`, `TestCardLogRootCommitOnlyForCardsInIt`, `TestCardLogTruncatedBefore`, `TestCommitsTouchingListsEveryCommitOfAPathOnce`, `TestCommitsTouchingExtendsWithoutRebuilding`, `TestCommitsTouchingRebuildsAfterResetAndShallowChange`, `TestMultiBackendCardLogFollowsMoveIntoOldDomain`; server `TestCardLogFromCommits` |
| G8 | The history walker visits the shallow boundary commit and does not cross it (no error on a depth-1 clone) | gitstore history | gitstore `TestWalkStopsAtShallowBoundary`, `TestWalkVisitsWholeHistoryWithoutShallow`, `TestWalkStopsWhenAsked` |
| G9 | Deepening to a date applies `unshallow` and lands exactly at the horizon; a second deepen goes further; past the root leaves no shallow entry | gitstore sync | gitstore `TestShallowCloneDeepenSince`, `TestShallowClonePushAndFetch`; server `TestDeepenSinceDecision` |
| G10 | Push rejection is detected by fetching, never by error type: nothing new → failed (commits kept, healthz age grows); new commits → re-apply and retry | gitstore sync | gitstore `TestRejectedPushIsDetectedByFetch`, `TestRejectedPushLeavesRemoteIntact`, `TestUnreachableRemoteKeepsCommits`; server `TestGitRejectedPushReappliesAndRetries`, `TestGitUnpushedAge` |
| G11 | Re-apply on a new tip replays the unpushed commits field by field onto the files as they now are — disjoint cards both land; same card, different fields → both land; same field → the replayed write wins, both commits in history; a create is idempotent by path (two replicas spawning the same iteration → one file, the loser's create is a no-op); a card the tip deleted stays deleted; the commits are found on the branch, so a restart between commit and push loses nothing; a tip sharing no history is refused | gitstore rebase | gitstore `TestRebaseReplaysDisjointFieldsOfOneCard`, `TestRebaseSameFieldReplayedWriteWins`, `TestRebaseUntouchedFieldsOfTheOtherSideSurvive`, `TestRebaseMergesNotesByID`, `TestRebaseCreateOnExistingPathIsANoOp`, `TestRebaseReplaysDeleteAndCreate`, `TestRebaseTheirDeleteWinsOverOurEdit`, `TestRebaseRosterFileFieldLevel`, `TestRebaseReplaysCommitsFromBeforeARestart`, `TestRebaseRefusesUnrelatedTip`, `TestRebaseFastForwards`; server `TestGitSyncPushesCommitsFromBeforeARestart` |
| G12 | A rank insertion touches one file; an exhausted key space rebalances only the run between the nearest roomy neighbours, in the same commit, never crossing into another domain | board rank + gitstore | board `TestRankBetweenOrders`, `TestRankBetweenOpenEnds`, `TestRankBetweenRefusesEqualOrInverted`, `TestRankNeverEndsInFloorDigit`, `TestRankBetweenRepeatedInsertsStayOrdered`, `TestRankRebalanceEvenAndBounded`, `TestRankNeedsRebalance`; gitstore `TestBackendMoveCardRewritesOneFile`, `TestMoveRebalancesTheRunWhenTheKeyIsExhausted` |
| G13 | Roster fragments from several domains merge into one order; equal ranks tie-break by id; duplicate names resolve to the oldest `created`, the rest are aliases whose cards still count; healthz names them | gitstore roster | gitstore `TestLoadAllMergesRostersInRankOrder`, `TestLoadTieBreaksByID`, `TestLoadAllDuplicateNamesResolveToOldest`, `TestLoadAllAliasProjectMergesColumns`, `TestLoadAllNoTeamFragmentOutsidePrimaryIgnored`, `TestMultiBackendIssuesReportAliasesAndGhosts`; server `TestHealthzReportsAliasesAndGhosts` |
| G14 | Domain follows the inheritance rule, LINKED CARDS FIRST, never a per-card choice: review card → reviewed card's domain (the edge: a closed-project original whose team lives in shared → closed, not shared); subtask → parent's even with its own column; iteration → task's; then project card → project's; then team card → team's; moving a card moves its review card and subtasks in the same action | board.DomainOf + gitstore MultiBackend | board `TestDomainOfLinkedCardsFirst`, `TestDomainOfProjectThenTeam`, `TestDomainOfUnknownReferencesFallThrough`; gitstore `TestMultiBackendCreateInheritsDomain`, `TestMultiBackendMoveCascadesToLinkedCards`; server `TestGitTwoDomainsMovePushesBothAndIsAdopted` |
| G15 | (deferred until `feat/card-mirrors` lands — not in this repository yet) Mirror refuses a target column in another domain (`ErrCrossDomain`); the guard order is pinned when the branch lands | boardservice.Mirror | 🆕 `TestMirrorRefusesCrossDomain` |
| G40 | A forge **throttling** the server is not a visitor losing access: GitHub answers a rate limit with the same 403 it uses for a repository one may not see (GitLab with 429, or 403 behind a proxy), and reading it as "no access" locked a visitor out of their own board for the cache's minute — every write refused. A refusal that names a spent budget or a retry delay is `ErrRateLimited`, an error like an outage: the visitor's last answer stands, a visitor never seen before gets an error, and nothing false is cached. Seen on the first prod deploy of the git backend | forge.throttled + forgeAccess.rights | forge `TestBeingThrottledIsNotTheSameAsHavingNoAccess`; server `TestForgeAccessKeepsTheLastAnswerWhenThrottled` |
| G42 | Rights are answered from what is already known while they refresh: past the TTL the visitor gets the last answer at once and ONE revalidation runs behind it per login, so the five requests a page load fires after a break do not queue behind a permission probe per domain; a visitor nobody knows anything about still waits for a real answer | server access | server `TestRightsAreServedWhileTheyRevalidate`, `TestAnUnknownVisitorStillWaitsForTheForge`, `TestForgeAccessCachesPerVisitorAndSurvivesOutage` |
| G43 | A sign-in that predates the scopes the board needs is a DEAD AUTHORIZATION, not a refusal: GitHub names a token's scopes in every answer, so a 404/403 on a token without `repo` drops the session (401 → the sign-in gate) instead of being cached as "no write access" until the visitor happens to sign in again; a token that carries `repo` is taken at face value, a forge that names no scopes tells us nothing, and a token the forge rejects during a background check is forgotten rather than served stale | forge github + server access | forge `TestATokenWithoutTheRepoScopeIsAStaleAuthorization`; server `TestADeadTokenIsForgottenRatherThanServedStale` |
| G44 | A fetch that finds nothing new keeps the cache current: with a git store, freshness is what the remote says, not what the clock says, so the sync re-stamps the cache and a board nobody has opened for an hour is not re-read from scratch when someone finally does; healthz names `cacheAgeSeconds` | server sync + boardstore | server `TestAQuietSyncKeepsTheCacheCurrent`, `TestHealthzNamesWhenTheCacheWasVerified` |
| G45 | Who else reads a domain is ONE question for everyone — the forge's collaborator listing, paged — not one per person, and the list a page needs is served from what is known while the refresh runs behind it (one per domain); a login nobody has ever asked about is still waited for, and a credential that may not list collaborators falls back to the per-login question | forge + server access | forge `TestGitHubReadersAreOneListingNotAQuestionPerPerson`, `TestGitHubReadersFallBackToOneQuestionPerPersonWhenTheListingIsRefused`; server `TestTheMemberListIsServedWhileItRefreshes`, `TestAPersonNobodyHasAskedAboutIsAskedAbout`, `TestForgeAccessReadersByCollaboratorPermission` |
| G46 | A card's team and project must live in the SAME repository: the project decides where the card lives (G14), so a team from another repository would leave the card where its own people cannot read it. The pair is refused where it is made — `SetTeam`, `SetEpic`, `CreateCard` and an epic moved between projects, 422 naming both repositories — never accepted and quietly ignored; rights are checked first for a move the visitor may not make at all. A name the roster does not declare yet decides nothing (it is declared in the card's own repository), a card that already carries such a pair stays editable in every other way, and the pickers stop offering the pair: the board names each team's and project's repository (`teamDomains`/`projectDomains`) and the menus narrow themselves, keeping what the card already carries so a legacy pair can be fixed | board domain + boardservice + apiserver + SPA | board `TestATeamAndAProjectFromDifferentRepositoriesAreARefusal`; boardservice `TestATeamFromAnotherRepositoryIsRefusedOnAProjectCard`, `TestAProjectFromAnotherRepositoryIsRefusedOnATeamCard`, `TestATeamAndAProjectInTheSameRepositoryArePermitted`, `TestCreatingACardWithATeamAndProjectFromDifferentRepositoriesIsRefused`, `TestANewTeamNameIsNotAConflict`, `TestALegacyConflictDoesNotFreezeTheCard`; apiserver `TestTheBoardNamesTheRepositoryOfTeamsAndProjectsOutsideThePrimary`; server `TestWriteNeedsWriteAccessToTheDomain`; web `repoconflict.test.ts` |
| G47 | The server credential on GitHub can be MINTED instead of kept: a GitHub App signs a JWT with its private key and trades it per repository for an installation token — scoped to the repositories the app is installed on, expiring within the hour, renewed before it runs out (5-minute slack), one mint at a time; the git transport asks for the token on every request, so a renewal between two pushes needs no re-wiring; a repository the app is not installed on is a plain refusal naming the fix; a repository naming its own static token keeps it, and the PAT mode (and GitLab) work as before; the sign-in default scope drops the Projects-v2 leftover `project` | forge app + server + cmd | forge `TestAppMintsInstallationTokensPerRepository`, `TestAppRefreshesAnExpiringToken`, `TestAppRefusesABrokenKey`, `TestAppGitAuthStampsAFreshTokenPerRequest`; server `TestReadersAreAskedWithTheAppInstallationToken`; cmd `TestGitConfigBuildsTheGitHubAppCredential` |
| G16 | The reviewer picker offers only logins that can read the card's domain | server people roster | server `TestBoardListsDomainsWithMembers`, `TestForgeAccessReadersByCollaboratorPermission`, `TestForgeAccessReadsPermissionsPerDomain` |
| G17 | An unreadable domain is absent from the snapshot AND the watch stream — no teams, projects, cards, placeholders or frames from it; an unreadable PRIMARY → 403, no board; a card whose team file is unreadable is served under its team name with the team's sprint controls unavailable | board.Visible + server visibleBackend + watch hub | board `TestVisibleDropsUnreadableDomainsEntirely`, `TestVisibleKeepsCardWhoseTeamIsUnreadable`, `TestVisibleNoDomainFollowsThePrimary`, `TestVisibleEverythingIsIdentity`, `TestNewBoardRecordsRosterDomains`; server `TestUnreadableDomainAbsent`, `TestUnreadablePrimaryIs403`, `TestWatchFilteredByDomain` |
| G18 | A newer `schema` is refused at startup; an older one is migrated in one commit | gitstore schema | gitstore `TestMigrateSchemaNewerRefused`, `TestLoadRefusesNewerSchema`, `TestMigrateSchemaOlderInOneCommit`, `TestMigrateSchemaWritesMissingBoardFile`; server `TestGitModeMigratesOlderSchemaAtStartup` |
| G19 | Remote changes reach the cache by tree diff: a pushed commit touching one card updates that card only and is broadcast once, to readers of its domain | server sync | gitstore `TestFetchBringsRemoteCommitAndDiffNamesTouchedPaths`; server `TestGitRemoteChangeReachesCache`, `TestWatchFilteredByDomain` |
| G20 | Restart keeps unpushed commits: commit without push, reopen the store → queued and pushed; `aeman mcp` drains on exit | gitstore + cmd | gitstore `TestUnpushedSurvivesReopen`, `TestUnpushedCommitsOldestFirst`, `TestRebaseReplaysCommitsFromBeforeARestart`; server `TestGitModeReopensTheClone`, `TestGitSyncPushesCommitsFromBeforeARestart`, `TestOpenGitBackendStandalone` |
| G21 | Repack + prune keep every object reachable | gitstore maintenance | gitstore `TestRepackKeepsHistoryReadable` |
| G22 | A cross-domain move is create-THEN-delete with the same id and one action id; the created file carries `movedFrom:`/`movedAt:` so a torn move resolves from the tree alone — a fresh depth-1 clone of both domains shows the card once; the create hits disk before the delete; maintenance removes the ghost after the destination landed | gitstore + boardservice | gitstore `TestMultiBackendRefilingIsAMove`, `TestLoadAllHidesTornMoveGhost`, `TestLoadAllPlainDuplicateIsNotAMove`, `TestMultiBackendLoadCardsPrefersMovedCopy`, `TestMultiBackendSweepGhosts`; server `TestGitTwoDomainsMovePushesBothAndIsAdopted`, `TestMaintenanceSweepsGhosts` |
| G23 | `Reopen` restores `doneFrom` regardless of history depth; no `doneFrom` → the in-progress nudge | boardservice.Reopen | boardservice `TestReopenRestoresDoneFromWithoutAnyHistory`, `TestReopenRestoresPreDoneProgress`, `TestReopenFallsBackWithoutHistory`; gitstore `TestServiceDoneAndReopenThroughTheBackend` |
| G24 | `serve` refuses an unborn remote naming `aeman init`; `init` bootstraps `board.yaml` + `teams/_.yaml` in one commit and is idempotent | cmd + gitstore | gitstore `TestInitBootstrapsAnEmptyRemote`, `TestCloneOfUnbornRemoteIsNamed`, `TestCloneWithoutBoardBranchIsUnborn`, `TestLoadEmptyRepositoryIsNamed`; server `TestGitModeRefusesUnbornRemote` |
| G25 | Every mutation requires `CanWrite` on its target domain(s); a read-only collaborator reads everything and changes nothing | server auth | server `TestWriteNeedsWriteAccessToTheDomain`, `TestCreateRosterEntryInChosenDomain` |
| G26 | Healthz reports the oldest unpushed commit age and turns `degraded` past `--unpushed-warn`; it also names the merge's aliases and the torn-move ghosts | server health | server `TestGitUnpushedAge`, `TestHealthzDegradesPastUnpushedWarn`, `TestHealthzReportsAliasesAndGhosts`; cmd `TestGitConfigFromFlagsAndEnv` |
| G27 | One board per server, named by its primary repository: `owner`/`board` request parameters are ignored, MCP tools take no board, and a server without `--repo` refuses to start | server + mcpserver + cmd | server `TestAPIIgnoresOwnerAndBoardParameters`, `TestGitModeServesTheConfiguredBoard`; mcpserver `TestMCPMissingBoardConfig` |
| G28 | Process turns are filed on the fetch tick, as the server identity, with deterministic iteration ids (the due week's Monday + a hash of task and week) — two replicas sweeping the same week write one file, the loser's create is a no-op | gitstore ids + server sync | gitstore `TestCreateIterationIDIsDeterministic`, `TestDeriveIDIsDeterministic`; server `TestSweepOnFetchTickFilesThisWeeksTurnsOnce` |
| G29 | A team, project or process is declared in the caller's chosen domain (`domain` in the body / MCP argument, else the context's, else the primary); an unknown domain is a 400 before any right is checked; a process with a project lives with the project | gitstore MultiBackend + server visibleBackend | gitstore `TestMultiBackendRosterDomainChoice`, `TestMultiBackendCreateHonoursDomainFromContext`; server `TestCreateRosterEntryInChosenDomain` |
| G30 | A create answers with the card's final id — minted before the write, never renamed on confirm; a roster entry created while a full reload was under way survives the reload | server queue + gitstore | server `TestGitCreateReturnsFinalID`, `TestInstallKeepsRecentRosterStubs`, `TestRevalidateRestoresCreatedCardInPlace`; gitstore `TestBackendCreateHonoursPremintedID` |
| G31 | A file the loader cannot parse is reported by path and skipped; the rest of the board loads | gitstore load | gitstore `TestLoadReportsBrokenFile` |
| G32 | Trailers parse robustly — unknown keys kept, malformed lines ignored, `-` stands for an empty side of an `Aeman-Change` | gitstore commit | gitstore `TestParseTrailersRobust`, `TestParseTrailersKeepsExtraKeys`, `TestCommitChangeTrailers` |
| G33 | CLI: `--repo name=url` repeatable (env `AEMAN_REPOS`), spans (`8w`, `1y`), committer `Name <email>`; every storage flag has an environment fallback and the flag wins | cmd | cmd `TestGitConfigFromFlagsAndEnv`, `TestRepoListParsesNameEqualsURL`, `TestParseSpan`, `TestParseCommitter` |
| G34 | A card log cut by the horizon deepens the clone on demand back to the card's creation, never past `--history-max`; the background deepen after start-up goes to `--history` | server sync | server `TestDeepenSinceDecision`, `TestCardLogFromCommits`; gitstore `TestShallowCloneDeepenSince` |
| G35 | The card resource carries `status.domain`; `GET /board` carries `members` with avatar URLs and the visitor's `domains` with `writable` and their readers | apiserver + server | apiserver `TestCardResourceCarriesDomain`, `TestBoardResourceMembers`; server `TestBoardListsDomainsWithMembers` |
| G36 | Issue/PR references in descriptions resolve with the server's credential, never the visitor's; without one they stay unresolved; answers are cached | server links | server `TestForgeLinksResolveIssueAndPullState`, `TestForgeLinksUnknownReferenceIsUnresolved`, `TestForgeLinksWithoutServerCredentialResolveNothing`, `TestForgeLinksCacheExpires` |
| G37 | Rights are the visitor's repository permissions per domain, decided from the forge, cached per visitor and kept through a forge outage; a token the forge rejects drops the session (401); readers of a domain come from its collaborator permissions | server access | server `TestForgeAccessReadsPermissionsPerDomain`, `TestForgeAccessCachesPerVisitorAndSurvivesOutage`, `TestForgeAccessRefusesABadToken`, `TestForgeAccessReadersByCollaboratorPermission`, `TestDropSessionForgetsIt` |
| G38 | Roster names — teams, projects, processes — are one namespace across every domain: the store refuses a create or a rename into a name any domain already declares (`ErrNameTaken`, 422), readable by the caller or not; repositories that already collide at start-up (two boards connected as one) are refused with both files named; the read-side alias merge (G13) covers only collisions made behind a running server's back. A team is renamed as one action — its file keeps the sprint pointer, every card and process task that names it follows; the no-team group has no name to rename | gitstore MultiBackend + boardservice.RenameTeam + server start-up | gitstore `TestMultiBackendRefusesRosterNamesTakenInAnotherDomain`; boardservice `TestRenameTeamRewritesPointerAndCards`, `TestRenameTeamRefusesTakenEmptyOrNoTeam`; server `TestAPIRenameTeam`, `TestRenameTeamReKeysTheCachedRoster`, `TestGitModeRefusesCollidingRosterNames`; mcpserver `TestMCPListsTools`; web `names.test.ts` |
| G39 | A card never names a team the board does not declare: a write that gives a card a team the roster lacks declares the team on the way — the sprint-joining create by recording its first sprint (as before), the sprint-less creates (a next-sprint card, a card for a later day) and a team assignment by writing a pointer-less `teams/<id>.yaml`; a team already declared keeps the pointer it has, the no-team group needs no declaring. Without it a card could carry a team that is on no roster and in no column (seen live when a browser offered a team remembered from another board) | boardservice.CreateCard + SetTeam (`declareTeam`) | boardservice `TestSprintlessCreateDeclaresAnUnknownTeam`, `TestSetTeamDeclaresAnUnknownTeam`, `TestCreateCardNoSprintSkipsFirstSprintRecord` |
| P9 | A personal board holds cards and nothing else: a team, project or process declared into a `~<login>` domain is refused (`ErrPersonalRoster`, 422) — the roster entry would sit in a repository nobody else can read, on a board with no place to show it. The store refuses whichever door the request came through (create, `SetSprintState`), and the UI does not offer a personal board among the places to declare one | gitstore.rosterDomain; web `writableDomains` | gitstore `TestMultiBackendRefusesRosterEntriesInAPersonalDomain`; vitest `writableDomains` leaves the personal board out |
| P1 | A personal board is a domain named `~<login>` — a `~` no configured repository is named with — linked by `users/<login>.yaml` in the primary (the primary's users files alone count); the domain is attached at run time when its owner first arrives and detached on unlink, the store's domain list taking and dropping it without a restart | gitstore roster/multi + server personal | gitstore `TestUserFileRoundTripAndPath`, `TestLoadAllUsersFromThePrimaryOnly`, `TestMultiBackendUsersFromTheLastLoad`, `TestMultiBackendPersonalDomainPinsItsCards`; board `TestPersonalDomainNaming`; server `TestPersonalBoardLinkCreateListUnlink`, `TestPersonalBoardIsAttachedWhenTheOwnerReturns` |
| P2 | A personal card is created so (`personal: true`) into the actor's own domain — assigned to them, with a zone, dates and a body; a team, column or plan band on it is refused (422), and an anonymous caller has no domain to file into | boardservice.createPersonalCard | boardservice `TestCreatePersonalCardGoesToTheActorsDomain`, `TestCreatePersonalCardRefusesPlacementAndAnonymity`; server `TestPersonalBoardLinkCreateListUnlink` |
| P3 | A personal card is pinned: the home rule never moves it out whatever team or project it is given; a card linked to it (subtask, review) follows it in; a personal create without an attached domain is refused, nothing is invented | gitstore MultiBackend | gitstore `TestMultiBackendPersonalDomainPinsItsCards` |
| P4 | The owner alone reads and writes their personal domain, whatever the forge says about the repository; everyone else's board and watch are without it, on `view=all` too | server access + board.Visible | server `TestPersonalBoardLinkCreateListUnlink` (bob's board, list and view=all), `TestPersonalBoardIsAttachedWhenTheOwnerReturns` |
| P5 | `view=personal` is the owner's repository as a backlog: every open card scheduled for today or earlier plus the ones done today; a card planned or deferred to a later day (start past the day) waits out of sight until that day, a card done on an earlier day, a card of another domain, another person's personal card — absent; the day defaults to today | board.PersonalView + apiserver | board `TestPersonalViewOpenAndDoneToday`; apiserver `TestSelectorPersonalView` |
| P6 | `doneAt` is the board day a write took a card to 100, cleared when it drops below — stored like `doneFrom`, never read from history — and the resource carries it as `status.doneAt`; the server's cache mirrors the rule so a read right after the write sees it | gitstore.SetProgress + server cache + apiserver | gitstore `TestSetProgressWritesDoneAtAndReopenClearsIt`; apiserver `TestCardResourceCarriesDoneAtAndPersonalDomain`; server `TestPersonalViewReseedsARecurrentCardTheNextDay` |
| P7 | A personal board has no carry-over, so its recurrent cards turn with the calendar when the **owner reads** `view=personal` — as of the real today, whatever `day` the read asks for (the `day` is a lens; looking at tomorrow creates nothing early): a finished recurrent card whose cycle came due is reseeded as a fresh copy (same title, zone and body, 0%, the owner's, in the personal domain) before the list answers. The default cycle means **every day** there — the copy is due the day after the card was finished — and a week/month cycle counts from the card's start day; never twice in one day (a card finished today, however late, rests until tomorrow), never once a fresh copy exists (same title, later start), never for a finished card without `doneAt`. Another person's read, a team card, `view=all` — no reseed | board.PersonalReseed + boardservice.ReseedPersonal + server/MCP list | board `TestPersonalReseedDueTheDayAfterAndByCycle`; boardservice `TestReseedPersonalSeedsTheNextIterationOnce`; server `TestPersonalViewReseedsARecurrentCardTheNextDay` |
| P8 | Planning on a personal board is dates alone: the calendar (`dates`) and the defer move a personal card's start and end exactly as a team card's — presses stack, a card created today takes a stale end date along — but the card **joins no sprint** on the way; the team rule (the sprint active on the start day, or the day itself) does not apply. The team rule itself is unchanged | boardservice.SetDates + Defer | boardservice `TestDatesOnAPersonalCardKeepItOutOfSprints` |
| P9 | The × on a personal card (remove) has no sprint to demote into: a card that has been **worked on** (progress above 0, finished included) and did not start today is **left behind on yesterday's board** — `leftAt` = yesterday on it and on its subtasks, nothing deleted; the personal view shows a left card on its `leftAt` day and before, not after. An untouched card, or one that started today, is deleted for real. Re-dating a left card (calendar, defer) clears `leftAt` on it and its subtasks — it is on the board again. The UI asks first when there is progress to lose (as the Team board does), on the Me board for team cards too: a Me card still in its team's current sprint is kept in the previous one, a personal card on yesterday's board | board.PersonalLeaves + PersonalView + boardservice.removePersonal/bringBack; removal.ts personalRemovalKind | board `TestPersonalLeavesWorkedCardsBehind`, `TestPersonalViewOpenAndDoneToday`; boardservice `TestRemoveOnAPersonalCardLeavesItBehindOrDeletes`; gitstore `TestLeftAtRoundTripsThroughTheCardFile`; server `TestRemovingAWorkedPersonalCardLeavesItOnYesterday`; vitest `personalRemovalKind`, `personalShows` |
| M1 | Migration: the final tree equals the snapshot byte for byte, whatever the event log says | migrate | migrate `TestFinalTreeEqualsSnapshot`, `TestEventsBecomeCommits` |
| M2 | Migration is idempotent: a second run on the migrated repository is a no-op without `--force` | migrate | migrate `TestSecondRunIsNoop` |
| M3 | Migration ids are deterministic and every id-valued field (`parent`, `reviewOf`, `task`, state links) is remapped; a dangling reference is cleared and reported | migrate | migrate `TestDeterministicIDsAndRemappedReferences`, `TestReportNamesDrops` |
| M4 | Migration reports everything it dropped or approximated: the `Status` field, issue cards reduced to `link`, unattributed notes, unapplied events, dangling references, and the id table | migrate | migrate `TestReportNamesDrops` |
| M5 | Legacy `PVTI_` ids resolve on the API and MCP for one major version through the migration's `githubId`; the ULID stays the key; an unknown legacy id → 404 | board.IsLegacyID + boardservice.findCard | boardservice `TestLegacyGitHubIDResolvesTheCard`; board `TestIsLegacyID` |
