apiVersion: v1
kind: Service
metadata:
  name: {{ include "aeman.fullname" . }}
  namespace: {{ include "aeman.namespace" . }}
  labels:
    {{- include "aeman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "aeman.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
