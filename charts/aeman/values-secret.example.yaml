# Copy to values-secret.yaml (gitignored) and fill in the real secret.
# `make apply` includes it automatically when present.
config:
  githubClientId: "Ov23li..."
  githubClientSecret: "<github-oauth-app-client-secret>"
